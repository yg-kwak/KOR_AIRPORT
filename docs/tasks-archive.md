# 작업 기록 (완료 이력)

> **완료한 작업만** 시간순(최신이 위)으로 기록한다. 진행중/백로그 같은 능동 작업관리는 하지 않는다
> (계획·논의는 대화/이슈/PR 로). 작업을 마치면 `/commit` 단계에서 아래에 한 줄 추가한다.
> 형식: `- [x] 리뷰 지적 반영(🟡-1·2·3·7) — 가져오기 개편 리뷰에서 나온 넷을 고쳤다. ① 공유 카드 컴포넌트가 스캔 뒤 부르는 기존카드 조회는 부가 정보인데, api 래퍼가 실패에 오류 토스트를 띄워 '번호는 읽었는데 스캔이 실패한 것'처럼 보였다 — api.get 에 quiet 옵션을 더해 조용히 넘기고, 컴포넌트가 요구하는 엔드포인트를 필수/선택으로 조각 주석에 명시했다. ② 가져오기 대상 목록이 장비 사용자 1명마다 DB 를 두 번 쳐서 수천 명이면 수천 질의였다 — 기관 매핑과 등록 여부를 각각 한 번에 읽어 2질의로 줄이고, 인원당 질의가 없는 것을 테스트로 고정했다. ③ 가져오기 전체가 한 트랜잭션이라 인원 수만큼의 장비 왕복 내내 커넥션을 붙잡았다 — 경계를 사람 단위로 옮겨, 한 명이 실패해도 그 사람만 되돌아가고 나머지는 계속 반영된다(멱등이라 다시 돌리면 이어진다). ④ 완전수식명을 import 로 정리 — sjpark2, 2026-08-18
- [x] 리뷰 권고 반영(🟡-1·2·3) — 허가 기간 리뷰에서 나온 셋을 고쳤다. ① 허가구역과 허가기간이 **같은 방문**에서 나온다는 규칙이 SQL 안에만 있어, 한쪽 쿼리가 바뀌어도 목 기반 단위 테스트는 초록이었다 — 방문 선택을 `<sql id="latestVisitOfPerson">` 조각 하나로 모아 두 쿼리가 함께 include 하게 하고, mapper XML 을 읽어 그것을 지키는 `MonitorPeriodRuleTest` 를 붙였다(규칙을 일부러 깨서 실패하는 것까지 확인했다). ② 인증 1건마다 인원 질의가 3회였다(`selectById` + 초 때문에 `selectAccessPeriod` + 기관명 `company.selectById`) — 화면 전용 `selectForMonitor` 하나로 합쳐 1회가 됐고, 기관명은 조인이 함께 준다. ③ `MonitorService` 가 469줄로 상한(500)에 가까워져 이벤트 보강을 `MonitorEnrichService` 로 떼어냈다(294줄 + 209줄, 매퍼 의존 5개 중 4개 이동). 클래스를 나누면 주입이 어긋나도 컴파일은 통과하므로 실제로 띄워 화면 5개(200)와 실시간 이벤트 요소를 확인했다 — sjpark2, 2026-08-25
- [x] 실시간 이벤트 [출입자 정보]에 허가 기간 추가 — 허가 구역 아래에 출입 허가기간을 띄운다. 출처는 구역과 같은 갈래로 갈린다(정규는 `tb_person.access_*`, 그 밖은 방문의 `tb_visit.work_*`). 방문객은 **구역 쿼리와 같은 방문**(가장 최근)을 보게 맞췄다 — 기준이 갈리면 구역과 기간이 서로 다른 방문 값으로 섞여 보인다(개발 DB 에서 두 기준 불일치 0건 확인). 정규·방문 같은 형식으로 초까지 쓴다(`2026-01-01 09:00:00 ~ 2037-12-31 23:59:00`) — 출입 판정이 초 단위라 경계에 걸린 사람을 볼 때 분까지로는 답이 안 나온다. 초는 DB 에서 실제로 읽는다: 목록·수정이 쓰는 `listColumns` 는 `datetime-local` 때문에 분까지여야 해 건드리지 않고 `selectAccessPeriod` 를 따로 뒀다(뒤에 `selectForMonitor` 로 합쳤다 — 아래 기록). 화면에서는 값이 41자라 모든 해상도에서 칸을 넘쳐(1920 기준 454/407px) 좁으면 두 줄로 접게 했는데, 붙임표가 줄바꿈 자리로 쓰여 날짜가 갈라지길래 시작·종료를 각각 span 으로 묶어 가운데 " ~ " 에서만 접히게 했다(1920 한 줄 / 1600·1366 두 줄, 넘침 0 실측) — sjpark2, 2026-08-25
- [x] 심화 테스트(deep-test.sh 234건) 추가 + 거기서 드러난 결함 4건 수정 — sjpark2, 2026-08-18
- [x] 요약 — 담당, 완료일, 커밋`

- [x] 실시간 이벤트 사진 칸 — 정규인원의 빈 사진은 사람 모양, 두 사진은 같은 크기로 꽉 채움 — 사진이 없을 때 늘 카드 그림을 세우고 있었다. 그러면 '얼굴 등록이 빠진 정규인원'과 '원래 얼굴을 안 찍는 카드 전용 인원(임시·장기·상주·순찰·대여)'이 화면에서 구분되지 않는다. 서버가 faceUser 를 내려주고 화면이 그림을 고른다(인원 구분은 우리 DB 사정이라 화면이 알 일이 아니다). 미등록 인증은 구분을 알 수 없어 카드 모양이다. 아울러 MAIN 의 두 사진을 같은 높이(260px)로 맞췄다 — 처음엔 cover 로 칸을 꽉 채웠는데, 실제 사진이 224x224/141x187 로 작아서 칸 가로(390px)에 맞추면 1.7~1.9배 확대돼 뭉개지고 세로 사진은 위아래가 잘렸다. contain + 고정 높이로 바꿔 확대 1.2배 안쪽에 잘림도 없앴다(지난 인증 띠는 별도 규칙이라 그대로). 가짜 이벤트를 흘려 넣어 실제 화면에서 확인했다(사람/카드 아이콘 분기, 두 사진 상자 387x260 동일) — sjpark2, 2026-08-18
- [x] BiostarX 가져오기 — 고른 사용자만, 이미 등록된 인원도 장비 기준으로 맞춘다 — 예전에는 대상 전체를 한 번에 끌어오고 이미 있는 인원은 건너뛰었다. 이제 [대상 불러오기]로 목록을 받아 고른 사람만 가져오고, 이미 있는 인원은 person_id 로 짝지어 카드·출입권한을 장비 기준으로 덮어쓴다(우리 쪽에만 있던 것은 떨어져 나간다). 얼굴은 사진이 바이너리라 비교가 무의미해 있고 없음만 본다 — 장비에 없으면 지우고, 둘 다 있으면 그대로 두고, 장비에만 있으면 가져온다. 인원정보는 장비가 원천인 컬럼만 갱신한다(일반 update 를 쓰면 생년월일·신원조회처럼 우리 화면에서만 채우는 값이 함께 비워진다). 실제 장비로 확인하다 유효기간 비교가 늘 '다름'으로 나오는 것을 잡았다 — 조회는 분 단위(varchar 16)로 읽는데 장비는 초까지 줘서, 아무리 가져와도 매번 갱신으로 집계됐다. 테스트 픽스처를 실제 조회 형식으로 바꿔 이 버그를 잡도록 했다. 미리보기 결과는 신규/갱신/변경없음 세 갈래로 사람을 나눠 줘서 화면의 구분 열·필터로 대상자를 바로 짚을 수 있고, 미리보기·가져오기 버튼은 검색줄(구분 필터 오른쪽)로 옮겼다. 이 과정에서 집계 구멍도 잡았다 — 카드나 출입권한만 바뀐 사람이 신규도 갱신도 변경없음도 아닌 채 어디에도 안 잡혔다(분류를 사람 단위 한 곳으로 모았다) — sjpark2, 2026-08-18
- [x] 주차 조회 메뉴(602) + 입·출차 이벤트 수신 — 주차된 차가 언제 들어오고 나갔는지 본다. 아마노는 조회 API 를 주지 않고 파트너사 주소로 밀어 주는 방식이라(POST /api/InOutCar) 지금까지와 방향이 반대다. 그래서 규칙이 다르다: 세션이 없어 보내는 쪽 IP 로 막고, 못 알아들은 요청에도 200 을 준다(500 을 주면 저쪽이 같은 건을 무한 재전송한다). 중복 수신은 정상이라 (유형·차량번호·시각) 유일키 + NOT EXISTS 로 한 줄만 남긴다. 규격이 늘어날 수 있다고 문서에 명시돼 있어 모르는 키는 무시하고 원문을 raw_json 에 보관한다. 화면은 기간·입출구분·미인식/미개방·차량번호로 조회하고, 출차 건은 함께 온 입차 시각을 같이 보여 준다. 외부 수신 경로라 화면 규약(@RequestMapping ↔ menu_url)의 예외를 code-lint·WebConfig 에 등록했다 — sjpark2, 2026-08-13
- [x] 주차 차단기(아마노) 정기권 연동 — 차량에 차량구역을 주면 그 구역 차단기가 열리도록 정기권을 등록한다(방문 차량은 방문 종료일까지, 기관차량은 상주라 2037-12-31 까지). 두 화면이 ParkingPassService 한 구현을 쓴다 — 매핑·회수 규칙이 갈리면 같은 차가 화면에 따라 다르게 열린다. 시험서버로 먼저 규격을 실증해 두 가지 전제를 바로잡았다: (1) 차량 1대에 정기권은 1건·종별 1개라 같은 차량 재등록이 "이미 등록된 차량" 으로 거부된다 → 등록은 항상 삭제 후 등록, (2) 거부도 HTTP 200 이라 성공 판정은 본문 data.success 로 한다. 구역이 빠지거나 방문·차량이 지워지면 정기권을 회수하고, 기관차량은 차량번호를 고치면 옛 번호도 지운다(안 지우면 그 차는 계속 들어온다). 주차관제 실패로 방문 저장을 롤백하지는 않는다 — 차단기는 부가 기능이라 여기서 막으면 등록 자체가 멈춘다(경고 + 감사). 차단기가 달린 구역만 종별(code_tag)을 갖는다 — 단말기가 한 대라 지금은 차량구역2 뿐이다. 그래서 고른 구역 중 '종별이 있는' 것을 찾는다: 정렬해 맨 앞을 집으면 차량구역1+2 를 고른 순간 태그 없는 1이 잡혀 등록이 통째로 빠진다(회귀 테스트로 고정). 종별이 붙은 구역이 2개 이상 겹치는 경우는 API 로 표현할 수 없어 미결로 두고, 잠정으로 앞선 하나만 등록하며 WARN 을 남긴다. 기본은 꺼짐이라 개발·시험이 현장 주차장을 건드리지 않는다 — sjpark2, 2026-08-13
- [x] 비활성 상태로 저장하면 얼굴도 지운다 — 출입만 막고 생체정보를 장비에 남겨 두면 상태를 되돌리는 순간 예전 얼굴로 문이 열린다. 상태(PS)의 code_tag='true'(정지·퇴사·회수·분실)면 BiostarX 의 사진·인증얼굴·템플릿을 비워 보내고 tb_person_photo 도 함께 지운다 — 한쪽만 지우면 다음 저장에서 되살아나므로 판정을 isDisabled 한 곳에 두고 양쪽이 쓴다. 되돌릴 수 없어 화면이 저장 전에 확인을 받는다(비활성 코드는 서버가 내려준다 — 화면에 박으면 상태가 늘었을 때 안내가 조용히 빠진다) — sjpark2, 2026-08-12
- [x] 리뷰 지적 반영(🔴-1·🟡-4) — 클라이언트 끊김 처리를 넣으면서 "응답이 이미 나갔다"와 "브라우저가 끊었다"를 한 갈래로 묶은 탓에, 엑셀 다운로드처럼 응답에 직접 쓰는 경로가 실패하면 ERROR 스택 대신 사실과 다른 INFO 한 줄로 조용해졌다. 두 갈래로 나눠 커밋된 응답의 진짜 오류도 스택을 남긴다(본문만 생략). 로그 레벨 자체를 테스트로 고정했다. 아울러 두 번 고쳤던 소속·허가구역 분기에 테스트 8건을 붙이고, 수정을 되돌려 실제로 실패하는 것까지 확인했다 — sjpark2, 2026-08-12
- [x] 인증 소리를 화면과 동시에 — 소리가 화면보다 늦다는 지적. 순서를 바꿔도 남는 지연이 있어 실제로 재 보니 원인은 브라우저 내장 음성이었다(speak() 부터 소리까지 첫 발화 1240ms, 이후 평균 1055ms — 엔진이 그때 말을 만든다). 미리 만든 음성 파일을 화면 로드 때 받아 디코딩해 두고 WebAudio 로 재생하도록 바꿔 0.2ms 가 됐다(화면 갱신 1.0ms 보다도 앞선다). 파일을 못 받으면 내장 음성 → 알림음으로 내려간다. 문구 변경은 파일 교체만으로 된다 — sjpark2, 2026-08-12
- [x] 실시간 이벤트 음성 안내 — 인증마다 "인증 성공"/"인증 실패"를 읽어 준다. 상황실은 화면을 늘 보고 있지 않아 소리가 먼저 알린다. 브라우저 내장 음성(speechSynthesis)을 쓴다 — 음성 파일을 두면 문구를 바꿀 때마다 파일을 다시 만들어야 하고 한국어 음성은 Windows 에 이미 들어 있다. 한국어 음성이 없는 PC 는 짧은 알림음으로 대신한다(성공은 높고 짧게, 실패는 낮고 길게). setting-form 오른쪽 끝의 스피커 아이콘으로 켜고 끄며 설정은 그 브라우저에 남는다 — sjpark2, 2026-08-12
- [x] 장치 선택 팝업을 공통 컴포넌트로 (devicePicker) — 실시간 이벤트의 단말기 선택을 사용자관리와 같은 팝업으로 바꿨다. 마크업을 복사하는 대신 사용자관리의 화면 지역 구현을 fragments/components 로 빼서 두 화면이 한 구현을 쓴다(목록 URL 은 인자 — 화면마다 자기 menu_id 로 권한을 본다). 선택하면 장치ID·장치명을 함께 보여 준다. picker-field 클래스는 쓰지 않았다 — [삭제] 버튼이 자동으로 붙는데 [중지]와 역할이 겹친다. 아울러 출입자 정보의 라벨·값을 한 그리드에 담아 값의 시작 위치를 맞췄다(줄마다 그리드를 나누면 "이름"과 "허가 구역"의 폭이 달라 어긋난다) — sjpark2, 2026-08-12
- [x] 실시간 이벤트 화면 다듬기 — 허가구역이 2122345 로 나오던 버그를 고쳤다(매핑된 하위 그룹까지 세었다. 하위는 부모의 ar_code 를 물려받으므로 그것으로 최상위를 찾아 중복 제거 → 12345). 출입자 정보 세 줄이 남는 높이를 1/3 씩 갖도록 하고(minmax(0,1fr) 이어야 긴 기관명이 제 몫보다 더 가져가지 않는다), 단말기 검색을 붙이고(목록은 한 번만 읽고 화면에서 거른다), 상태 표시를 [중지] 오른쪽으로 옮기며 최근 시각 표기를 뺐다. 지난 인증 카드는 값이 없어도 이름·소속·허가구역 세 줄을 '-' 로 세운다 — sjpark2, 2026-08-12
- [x] 실시간 이벤트 — 밤새 쌓이던 ERROR 제거 + 1분 지나면 MAIN 비움 — 롱런 테스트에서 7~9분마다 ERROR 스택이 쌓였다. 원인은 브라우저가 SSE 스트림을 끊은 뒤 연결유지 신호를 쓰다 난 CloseNowException 이었고, 이걸 500 으로 다루면서 오류 본문(JSON)을 text/event-stream 응답에 실으려다 두 번째 실패까지 났다. 끊김을 알아보고 INFO 한 줄만 남긴다. 끊김 자체는 한산할 때만 났다(실제 인증이 흐르기 시작한 07:26 이후 24분간 0회) — 기능 영향은 없다. 아울러 마지막 인증을 MAIN 에 1분만 두고 그 뒤엔 지난 인증 띠로 내린다(지나간 사람이 계속 커다랗게 떠 있으면 방금 지나간 사람으로 읽힌다) — sjpark2, 2026-08-12
- [x] 실시간 이벤트 모니터링 화면 (모니터링 900 / 실시간 이벤트 901) — 단말기를 고르면 BiostarX 인증을 실시간으로 띄운다. 서버가 wss 소켓 하나를 열어 SSE 로 화면에 밀어 준다(브라우저는 self-signed 인증서와 세션 때문에 장비 소켓을 직접 못 연다). 소켓 인증은 헤더가 아니라 본문 첫 메시지 `bs-session-id={세션}` 이고, 그 응답(code 0)을 받은 뒤에 events/start 를 불러야 이벤트가 흐른다 — 순서가 바뀌면 아무 오류 없이 이벤트만 안 온다. 표기는 코드표(4102·4106·4867 통과 / 6401·6405 거부)로 서버가 정하고, 거부면 초록이 붉게 바뀐다. 허가구역은 정규는 tb_person_ac_group, 방문객은 tb_visit_ac_group — sjpark2, 2026-08-11
- [x] 실시간 이벤트 상시 운용 대비 — 상황실에서 하루 종일 켜 두는 화면이라 "죽은 줄 모르는 것"을 없앴다. 이벤트 수신 가능 여부를 소켓 존재가 아니라 세션·events/start 성공까지 포함해 판정하고(실패 사유가 화면까지 간다), 어느 단계에서 실패하든 소켓을 버리고 재연결한다. 구독자 목록과 소켓 수명을 한 잠금으로 묶어 교대 경합을 없애고, 웹 세션 1시간 만료는 화면의 5분 주기 갱신으로, BiostarX 세션 idle 만료는 3분 주기 능동 확인(세션 ID 변화까지 비교)으로 덮는다. 스케줄러 풀·구독 감사 중복·보강 대기열 상한·HTTP/2 도 함께 — sjpark2, 2026-08-11
- [x] 방문객은 카드만으로 인증하도록 개인 인증 모드 전송 — 임시·장기·상주·순찰·대여는 얼굴을 등록하지 않는데 장비 기본 모드가 얼굴을 요구하면 카드를 대도 문이 열리지 않았다. 사용자 payload 에 private_operation_modes(operation_mode 21 = 카드만)를 함께 보낸다. 정규인원은 얼굴+카드라 보내지 않는다 — 붙이면 장비·사용자그룹 설정을 덮어써 얼굴 인증이 막힌다. 방문객은 전부 VisitBiostarService 를 지나므로 인원구분 분기 없이 서비스 경계로 갈린다 — sjpark2, 2026-08-11
- [x] 가져오기 집계에서 범위 밖 인원 제외 — '장비 35명 · 대상 8명 · 가져올 수 있음 -27명' 처럼 음수가 나오고, 우리 대상이 아닌 임시·장기 사용자가 '건너뜀'으로 잡혔다. 범위 밖은 세지도 사유를 남기지도 않는다. 대상=정규등록 그룹 아래 인원, 가져올 수 있음=선별 통과, 건너뜀=대상 중 못 가져오는 것 — sjpark2, 2026-08-10
- [x] BiostarX 가져오기 범위를 정규등록 그룹 아래로 한정 — 장비에는 임시·장기 사용자도 있어 전체를 끌어오면 정규가 아닌 사람이 섞였다. 발급구분 PTD01 의 code_tag 를 뿌리로 하위 그룹까지 넓혀 그 안의 사용자만 가져온다. 화면 트리용 그룹 조회는 일부만 주므로 평면 전체 목록을 따로 읽는다 — sjpark2, 2026-08-10
- [x] BiostarX 정규인원 가져오기 — 현장 장비에 이미 있는 사용자를 우리 DB 로 옮긴다. 읽기 전용 단방향(BiostarImportAdapter) — 우리 등록 흐름으로 맞추면 장비의 얼굴·카드·출입그룹을 덮어쓴다. 기관(사용자그룹) 매핑이 있는 인원만, 이미 등록된 인원은 건너뛰고, 출입그룹은 매핑된 것만. 카드·얼굴·출입권한은 화면에서 체크해 고른다. 미리보기로 대상과 건너뛴 사유를 먼저 본다 — sjpark2, 2026-08-07
- [x] 리뷰 지적 반영(🔴-1·🟡-1~4) — 장기 화면의 [신청서 출력] 숨김(임시 전용 양식), VisitPermitTest 9건 추가, 실패 payload 마스킹을 JSON 파싱으로 바꿔 card_type.name 유지, 감사유형 이름을 tb_common 한 곳으로 되돌리고 유형 드롭다운 제외는 systemActions 조각이 담당, 파기 모드를 기동 로그에 한 줄 — sjpark2, 2026-08-07
- [x] 신청서 표기 보정 — 출입증번호 칸에 카드번호 대신 카드명칭을 넣고(회수됐으면 마지막 카드로 되짚어 찾는다), 출입자소속에서 주소 자리의 '/' 를 뺐다(입력란이 없다) — sjpark2, 2026-08-07
- [x] BiostarX 실패 요청의 본문을 로그에 남긴다 — 응답만으로는(not defined) 무엇이 잘못됐는지 알 수 없었다. HTTP 400 이상일 때 보낸 본문을 적되 성명·사진·연락처는 가리고 user_group_id·access_groups·cards 등 구조는 남긴다. 성공과 401(세션 만료 재시도)은 제외. BiostarSession 한 곳이라 모든 호출에 적용된다 — sjpark2, 2026-08-07
- [x] BiostarX 동기화 실패인데 상태가 입실 중으로 바뀌던 문제 — 카드가 문을 열지 못하는데 처리가 끝난 것처럼 보였다. 동기화 실패 시 신청(VS01)으로 되돌리고 사유를 화면·감사에 남긴다(저장은 유지 — 장비에 부분 생성된 사용자를 유령으로 남기지 않는다). 원인은 발급구분(PTD)의 사용자그룹 ID 가 장비에 없어서(code 65717 not defined) — integration.md 에 기록 — sjpark2, 2026-08-07
- [x] 출입허가 신청서 출력(임시인원등록) — 수정 모달에 [신청서 출력] 추가(신청 상태에는 출입증번호가 없어 숨김). 서버는 PermitForm 값만 주고 화면이 양식을 그려 브라우저 인쇄로 넘긴다(프린터가 클라이언트 PC). 출입구역은 번호만(1,3,5), 출입자·인솔자는 2명씩 행을 늘린다. 확인자·근무확인·운전자·주소는 보관하지 않는 값이라 빈칸 — sjpark2, 2026-08-07
- [x] 감사추적 [시스템] 필터가 통째로 무시되던 버그 — SystemLogSearchParam.systemOnly 에 게터가 없었다. Lombok 을 쓰지 않는 프로젝트라 요청 바인딩도 MyBatis 조건 평가도 게터로 하는데, 필드만 추가해 값이 늘 false 였다(오류 없이 전체 조회). 게터를 넣고 ArchUnit 규칙으로 같은 실수를 막는다. 실제 로그인해 API 를 호출하는 방식으로 검증 — sjpark2, 2026-08-07
- [x] 시스템 사건은 메뉴 [시스템] 한 곳에서만 고른다 — 유형(AT) 목록에도 넣었더니 고를 곳이 두 군데가 됐다. AT 코드는 지우고 유형 칸 이름만 mapper 가 붙인다. 아울러 정적 리소스에 Cache-Control: no-cache 를 걸었다 — 없으면 jar 를 새로 올려도 브라우저가 옛 화면 코드를 계속 써 검색 조건이 어긋난다(이번 [시스템] 필터가 안 먹던 원인) — sjpark2, 2026-08-07
- [x] 감사추적 [시스템] 을 시스템 사건만으로 좁히고 기동·종료 기록 추가 — menu_id 없음 + user_id='SYSTEM' 으로 판정하니 키오스크 신청·계정 잠금처럼 사람이 한 일까지 묶였다. 판정을 감사유형(STARTUP/SHUTDOWN/PURGE)으로 바꾸고, SystemEventService 가 기동(ApplicationReadyEvent)·종료(ContextClosedEvent)를 남긴다. 기록 실패가 기동을 막지 않는다 — sjpark2, 2026-08-07
- [x] 정기 파기 이력을 감사유형 PURGE(자동 파기)로 분리 — 메뉴 [시스템]으로 걸러도 키오스크 신청·계정 잠금이 섞여 배치 이력만 보기 어려웠다. 사람이 지운 DELETE 와 다른 유형으로 남겨 감사추적 유형 필터로 정확히 뽑는다 — sjpark2, 2026-08-07
- [x] 감사추적에서 [시스템] 기록을 검색할 수 있게 — 메뉴 칸에는 [시스템]으로 보이는데 실제 메뉴가 아니라 검색 조건에는 없었다. 메뉴 드롭다운에 고정 항목으로 넣고 systemOnly 플래그로 거른다(menu_id 가 없어 숫자 menuId 로는 보낼 수 없다). 정기 파기·키오스크 신청·계정 잠금이 여기 걸리고, 로그인처럼 사용자가 한 일은 걸리지 않는다 — sjpark2, 2026-08-07
- [x] 2026-08-07 DB 변경분 단일 스크립트 — 운영 DB 에 한 번에 넣도록 sql/deploy/2026-08-07_update.sql 작성. 코드 4종(VS05·PS 05/06·PT05/06·PIP PL/RT)과 tb_visit.checkout_dt 추가+백필. 멱등이며 마지막에 적용 결과를 표로 확인한다 — sjpark2, 2026-08-07
- [x] 방문 데이터 정기 파기 — 퇴실 완료 후 1년이 지난 임시·장기 방문을 통째로 물리 삭제한다(방문/인솔자매핑/방문객/차량/출입구역). BiostarX 사용자도 함께 지우되 장비를 먼저 지운다(DB 를 먼저 지우면 어떤 사용자를 지울지 알 수 없다). 기준 시각용 tb_visit.checkout_dt 추가 + 기존 퇴실완료분은 mod_dt 로 백필. 인솔자(정규인원)·기관차량·다른 방문에도 있는 인원은 제외. 방문에서 빠져 남은 방문객도 같은 기간으로 파기. 0건이어도 감사에 실행 이력을 남기고 메뉴는 [시스템]으로 보인다. 기본은 미리보기(dry-run) — sjpark2, 2026-08-07
- [x] 리뷰 지적 반영(🔴-2·🔴-3) — 인원 삭제 시 얼굴 사진(tb_person_photo)을 함께 지운다. 남겨 두면 소프트 삭제 상태로 생체정보가 보관되고, 인원ID 를 재사용할 때 다음 사람 화면·카드에 앞사람 얼굴이 나온다. 되살리기 경로에서도 한 번 더 지워 이 수정 전 삭제분을 처리한다. 문서의 현장 서버 IP 는 <서버IP> 자리표시자로 교체 — sjpark2, 2026-08-07
- [x] 공통코드 3종 추가 — (1) 인원상태(PS) 재발급 '05'(code_tag='false' 활성)·분실 '06'('true' 비활성). (2) 인원구분(PT) 순찰 PT05·대여 PT06 — 장기·상주와 같은 발급구분(PTD03)+세부 출입구역('Y')이라 장기출입등록 방문유형 select 에 자동으로 뜬다. 인원ID 접두(PIP)는 순찰 PL·대여 RT. (3) 정규인원 출입종료일 상한을 2028-05-31 로 — 계약 기간이라 설정(app.person.access-end-max)으로 빼고 화면·서버가 같은 값을 쓰게 했다(BiostarX 상한 2037-12-31 을 넘으면 깎는다) — sjpark2, 2026-08-07
- [x] 마지막 방문객이 퇴실하면 방문도 자동 퇴실 완료 — 개별 퇴실만으로는 방문이 계속 입실 중(기간이 지나면 미반납)으로 남아 [퇴실]을 또 눌러야 했다. 남은 방문객이 0이면 차량 카드까지 회수하고 VS04 로 마감한다(방문 전체 퇴실과 같은 마무리) — sjpark2, 2026-08-07
- [x] 미반납(VS05) 상태 추가 — 입실 중인데 작업기간이 끝난 방문을 카드 회수 대상으로 표시한다. **저장하지 않고 조회할 때 계산한다**(TbVisitMapper.statusExpr) — 처음엔 스케줄러로 일괄 전환했으나 다음 실행 전까지 화면이 틀린 값을 보여주고 상태가 두 곳(코드·DB)에 생겨 되돌렸다. 목록·상세·상태검색·상태명 조인이 같은 식을 쓰고, 저장 시에는 VS03 으로 되돌려 적는다. 퇴실은 VS03·VS05 둘 다에서 가능(카드를 돌려받아야 하므로). BiostarX 는 건드리지 않는다 — 작업기간이 곧 장비 유효기간이라 이미 출입이 막혀 있고, 장비 정리는 실제 반납 시점(퇴실)에 한다 — sjpark2, 2026-08-07
- [x] 삭제한 인원ID 재등록 · 카드 미발급 방문이 갇히던 문제 — (1) 정규인원은 소프트 삭제라 같은 번호로 다시 등록하면 "이미 존재하는 인원ID"로 막혔다. 남은 행을 되살리는 revive 를 추가한다. (2) 임시 방문객을 카드 없이도 BiostarX 에 올리고 있어, 신청 상태인데 삭제는 '등록된 방문객이 있다'고 막히고 퇴실은 '입실 중'이 아니라 못 해 방문이 갇혔다. 전원 카드 발급 시에만 BiostarX 에 올리고 입실 중으로 승격하며, 미발급 인원수를 저장 메시지로 알린다 — sjpark2, 2026-08-06
- [x] 기관차량등록이 현장에서 안 열리던 문제 — STRING_AGG 은 MSSQL 2017+ 전용인데 현장 서버는 2016 이라 "인식할 수 없는 기본 제공 함수 이름" 오류가 났다. FOR XML PATH 로 바꾸고, 최소 지원 버전(2016)을 database.md 에 명시, code-lint [7] 로 2017+ 함수 사용을 막는다 — sjpark2, 2026-08-06
- [x] BiostarX 연결 테스트 SSL 오류·동일 서버 세션 충돌 해결 — (1) self-signed 인증서 SAN 에 서버 IP 가 없어 호스트명 검증에서 핸드셰이크가 깨졌다. trust-all 과 별개라 jdk.internal.httpclient.disableHostnameVerification 로 푼다. (2) 쿠키는 포트를 구분하지 않아 같은 서버의 BiostarX 가 JSESSIONID 를 덮어써 로그아웃됐다 — 쿠키 이름을 CJAIRPORT_SESSION 으로 분리. (3) 연결 테스트 실패 문구를 조치 가능한 한국어로 바꾸고 요청 URL·HTTP 상태를 로그에 남긴다 — sjpark2, 2026-08-06
- [x] 로그를 하루 한 파일(cjairport.{날짜}.0.txt)로 — properties 만으로는 '지금 쓰는 파일'이 고정 이름이라 오늘 로그를 이름으로 찾을 수 없었다. logback-spring.xml 에서 <file> 없이(file-less) 날짜 파일에 바로 쓴다. 현장 설정은 LOG_PATH·LOG_KEEP_DAYS 둘뿐 — sjpark2, 2026-08-06
- [x] 일반 로그와 접근 로그를 한 파일로 통합 — Tomcat 접근 로그(별도 파일·AccessLogConfig)를 걷어내고, 요청 로그를 MdcFilter 에서 남긴다(REQUEST 로거). 이미 요청 전체를 감싸는 필터라 처리시간도 여기서 재고, 같은 요청의 업무 이력·오류·HTTP 결과가 [요청ID] 로 묶여 한 파일에서 이어 보인다. 화면 리소스(css·js·ico)는 제외 — sjpark2, 2026-08-06
- [x] 업무 이력을 파일 로그에도 기록 + 로그 설정 축소 — ①메뉴 접속·조회·등록·수정·삭제·로그인이 DB(tb_system_log)에만 남고 파일에는 없던 것을 AuditService 한 곳에서 'AUDIT' INFO 로거로 함께 기록(호출 지점이 이미 모여 있어 서비스 코드 무변경). ②접근 로그 세부(형식·접두·보관·버퍼)를 config/AccessLogConfig 로 옮겨 properties 에는 켜고 끄기 한 줄만 — 운영 설정 파일의 로그 항목 17줄 → LOG_PATH 1줄 — sjpark2, 2026-08-06
- [x] 접근 로그 추가 — '로그가 안 쌓이고 자정에 새 파일도 안 생긴다'는 신고 조사. 설정·롤오버는 정상이었고, 원인은 정상 조회·저장이 INFO 레벨에서 로그를 한 줄도 남기지 않는 것(로그 이벤트가 없으니 롤오버도 안 일어남 — 실제로 요청 1건을 넣자 즉시 전날 파일이 생성됨). 요청 1건=1줄인 Tomcat 접근 로그를 켜서 가동 여부를 파일로 확인할 수 있게 했다(%D는 Tomcat10에서 마이크로초라 %{ms}T 사용, 세션ID는 유출 시 탈취 위험이라 제외) — sjpark2, 2026-08-06
- [x] 카드구분 교차 배정 차단 + 기관차량 카드 불러오기 — ①정규인원 할당하기 팝업이 차량 카드까지 내려주던 문제(cardType 미전달). 목록 필터 + 서버 가드(requireIssuableToPerson/ToCar) 양쪽에 적용 — 직접입력·SCAN 우회 시 카드구분이 덮어써져 차량에서 카드를 빼앗던 경로도 차단. ②기관차량 카드 발급에 [불러오기] 추가(미할당 차량카드 선택). 카드 선택 팝업 마크업을 공용 조각으로 추출해 방문·기관차량이 공유 — sjpark2, 2026-08-05
- [x] 카드 차단·발급 제한이 동작하지 않던 원인 3건 — ①seed 가 CS 코드의 code_tag 를 채우지 않아 신규 설치 시 차단·발급제한이 전부 무력화(isBlocked 항상 false). seed·설치 스크립트에 값 추가 + 기존 DB 보정 절. ②장비 미등록 인원카드를 차단하면 조용히 성공 처리되던 문제(biostar_card_id NULL 이면 skip) — 이제 사유와 함께 거부. ③카드상태를 화면에서 고를 수 있어 새 카드를 처음부터 '분실'로 발급할 수 있던 경로 차단(requireIssuableStatus 신설, 카드등록관리 마스터는 제외). 테스트 2건 추가(총 79) — sjpark2, 2026-08-05
- [x] 카드 발급 제한·차량 중복 차단·기간 기본값 — ①정상(CS.code_tag≠'Y')이 아닌 카드는 신규 발급 불가(정규인원·방문객·방문차량·기관차량 전 경로 + 선택 팝업에서도 제외, 보유 중이던 카드는 예외). ②한 방문에 같은 차량번호 중복 등록 차단(공백·대소문자 무시). ③임시·장기 목록 기간 기본값 전체→1개월(첫 조회부터 적용). CardService 500줄 한도로 검증·발급규칙을 CardIssueService 로 분리. 테스트 8건 추가(총 77) — sjpark2, 2026-08-05
- [x] 전반 점검 후속 3건 — ①검색·정렬 보조 인덱스 6개 신설(감사추적 reg_dt, 카드 person/car, 인원 type, 방문 search, 방문객 person). PK 외 인덱스가 UX_tb_card_value 하나뿐이던 상태 해소. ②계정 잠금 실제 구현(5회 연속 실패 → 잠금, 잠긴 계정은 올바른 비밀번호도 거부, 사용자관리 [잠금해제]·비밀번호 재설정으로 해제, 감사 기록). 문서에만 있고 코드에 없던 공백. ③HikariCP 풀 명시(기본 10 → 30, 타임아웃·누수감지). BiostarX 동기화가 트랜잭션 안이라 기본값이면 등록 10건에 전체가 멈춤. 테스트 6건 추가(총 69) — sjpark2, 2026-08-04
- [x] 임시·장기 방문 목록 번호 정렬 수정 + 전 화면 정렬 점검 — TbVisitMapper orderBy 에 visitNo 분기가 없어 otherwise(고정 DESC)로 떨어져 오름차순이 먹지 않았다. 우연히 otherwise 와 일치해 동작하던 personId·cmmId·userId·companyCode 도 명시. 재발 방지로 code-lint [6](data-sort ↔ mapper sort 키 일치) 추가. 목록 9화면 정렬 헤더 37개 전수 확인 — sjpark2, 2026-08-04
- [x] 정규인원 목록에 상태·얼굴등록·카드 컬럼 추가 — 출입기간 오른쪽에 배지로 표시. 얼굴등록은 tb_person_photo 존재 여부(사진 본문은 읽지 않음), 카드는 살아있는 tb_card 장수. badge.personStatus(PS 코드→톤) 신설 — sjpark2, 2026-08-04
- [x] 임시·장기 방문 검색에 '출입시작 기간' 추가 — 감사추적과 같은 공통 period 컴포넌트(전체/1·3·6개월/1년/직접입력). 업무 목록이라 기본은 '전체'(period.range('all')=빈값 → 서버가 기간 조건 미적용), work_start_dt 기준 — sjpark2, 2026-08-04
- [x] 방문상태 '신청 취소'(VS02) 제거 — 설정 경로가 없어 쓰이지 않던 상태값. 신청 단계는 취소 대신 삭제로 처리. 설치 스크립트 보정 절에 기존 DB 정리(참조 있으면 보존) 포함 — sjpark2, 2026-08-04
- [x] 임시·장기 방문객 탭에 인원ID 표시 — 방문객이 BiostarX 사용자로 편입되면 부여되는 인원ID(=biostar_user_id)를 방문객 탭 성명 왼쪽에 읽기전용 컬럼으로 노출(신규는 '저장 후 부여'). 성명·생년월일·카드 폭을 줄여 공간 확보. detail 이 이미 personId 반환하므로 프론트만 변경(visitor.html/js 공유) — sjpark2, 2026-07-28
- [x] 운영 설정 예시 파일 추가 — deploy/application.properties.example(전 키 설명 포함, 경로/포트/로그/SSL/DB/ARIA). jar+config 배치로 실제 기동·HTTPS·파일로그 생성까지 검증 — sjpark2, 2026-07-30
- [x] 인솔자 선택 팝업에 [검색] 버튼 추가 — 엔터로만 검색되던 것을 카드 선택 팝업과 같은 형태로 통일 — sjpark2, 2026-07-30
- [x] 방문객 인원ID 접두 유형별 분리 — 임시 IS / 장기 LT / 상주 RS, 접두는 공통코드 PIP 에서 읽고 시퀀스는 접두별 독립. 유형 추가 시 코드 수정 없이 공통코드만 추가. 기존 IS 는 소급 변경하지 않음. 테스트 2건 — sjpark2, 2026-07-30
- [x] 얼굴 템플릿 길이 오류 수정 — 장비 등록값(544B)보다 6바이트 긴 템플릿이 전송돼 인증이 실패하던 문제. 널 꼬리 제거 대신 544바이트로 절단(버퍼 끝이 널이 아닌 잔여값인 경우 대응). 실제 관측 데이터로 회귀 테스트 2건 — sjpark2, 2026-07-30
- [x] 얼굴 인증 실패 원인 수정 외 3건 — 파일 업로드가 원본을 인증용 얼굴 자리에 넣던 문제(정규화 이미지로 교체, 사진은 원본 유지), 장치 변경 즉시 반영(dev_id 를 DB 에서 조회), 성명 자동완성 차단, 차량 불러오기 검색창 폭 — sjpark2, 2026-07-30
- [x] 현장 적용 준비 — 파일 로그 활성(LOG_PATH 로 경로 지정, 일자+50MB 분할·30일·1GB 보관: 서비스 등록 시 콘솔 로그 소실 방지), 현장 설치용 sql/deploy/CJAirPort_install.sql(SSMS 단일 파일 — DB생성·DDL·기본데이터·보정, 재실행 안전) — sjpark2, 2026-07-30
- [x] 방문객 인원ID 는 BiostarX 생성 후에만 표시 — 키오스크 신청만 한 방문객에게 채번된 ID가 그대로 보이던 문제. biostar_user_id(동기화 성공 시에만 채워짐)를 기준으로 표시하고 그 전에는 '등록 전' 배지 — sjpark2, 2026-07-30
- [x] 임시인원등록 로직 보정 4건 — BiostarX 등록 방문객이 있으면 방문 삭제 차단, 퇴실은 입실 중에서만(신청은 제거), 퇴실 버튼은 저장된 카드일 때만 노출(카드 선택 즉시 바뀌던 오류), 퇴실자 때문에 카드 교체가 막히던 오류 수정, 방문 전체 퇴실도 방문객별 퇴실시각 기록. 테스트 4건 — sjpark2, 2026-07-30
- [x] 방문 카드 팝업의 '카드 없음' 버튼 제거(발급 or 미발급 둘 중 하나, 회수는 [퇴실]이 담당) + 방문객 관리 칸 폭 80→160px, 퇴실 배지·시각을 한 줄로 표시 — sjpark2, 2026-07-30
- [x] 방문객 개별 퇴실 — 카드 발급된 방문객은 제거 대신 [퇴실](BiostarX 비활성화 성공 후 카드 회수·checkout_dt 기록), 퇴실자는 카드 재발급 불가(화면·서버), 저장 재구성에도 퇴실 이력 보존. 카드 선택 팝업을 공용 컴포넌트로 분리. 테스트 55건 — sjpark2, 2026-07-30
- [x] 카드 보유 방문객 제거 차단 + 처리 중 표시 — 카드를 발급받은 방문객은 회수 후에만 행 제거 가능(안내 토스트), 저장·삭제 등 서버 왕복 시 전역 진행 표시(js/core/busy.js, 0.25초 지연·중복 클릭 차단) — sjpark2, 2026-07-30
- [x] 방문객 제거 시 BiostarX 사용자도 삭제 + 카드 중복 발급 차단 — 저장 때 폼에서 빠진 방문객이 장비에만 남던 문제(실패=롤백), 같은 카드를 두 명에게 주면 서버 거부, 카드 선택 팝업에서 이미 고른 카드 숨김. 테스트 3건 — sjpark2, 2026-07-30
- [x] 정규인원등록 검색어 문구 통일 + 검색 입력이력 노출 차단 — placeholder '검색어를 입력하세요'(성명 완전일치 안내는 select 옵션으로 이동), 전 화면 검색 입력에 autocomplete="off" 템플릿 명시 — sjpark2, 2026-07-30
- [x] 코드 리뷰(2026-07-30) 🟡 5건 반영 — 카드 탭 코드검증 추가, 사진검증 테스트·MIME 디코더, 기존 값은 통과하는 코드검증(코드 정리 후 기존 행 수정 차단 해소), 엑셀 실패 사유 전체 표시, 사용유무 강제 감사 기록. 테스트 50건 통과 — sjpark2, 2026-07-30
- [x] 기관 차량 모달 가로폭 확대 — 640px 에서 고정폭 합계(620px)에 밀려 차량명칭이 세로로 접히던 문제. 전용 폭 900px + 표 가로 스크롤 래퍼(modal-table-scroll), 차량번호·관리자 폭 축소 — sjpark2, 2026-07-30
- [x] 엑셀 일괄등록의 없는 공통코드 차단 + 본인 계정 삭제 방지 — CodeValidationService 신설(카드 CDT/PT/CS · 정규인원 UT/PS · 기관 CO 존재·사용 검증, 실패 행은 사유와 함께 안내), 사용자관리 목록에서 로그인 계정은 삭제 버튼 미노출(서버는 기존대로 거부). 테스트 4건 — sjpark2, 2026-07-30
- [x] 시스템 공통코드의 사용유무를 '사용' 고정 — 미사용 전환 시 업무 화면에서 코드가 사라지는 것 방지. 수정 모달 비활성화 + 서버 강제(CommonService.update), 테스트 2건 — sjpark2, 2026-07-30
- [x] 카드등록관리 검색어에 발급대상 추가 — searchType=holder(인원ID·차량번호 부분검색), 전체(all) 분기에도 포함. count 쿼리에 tb_car 조인 보강 — sjpark2, 2026-07-30
- [x] 정규인원 삭제 시 카드 자동 회수 — 삭제해도 카드가 '발급중'으로 남아 재발급이 불가하던 문제. 삭제 트랜잭션에서 카드 회수·출입권한 정리 추가(BiostarX 삭제 실패로 롤백되면 회수도 안 됨), 테스트 2건, 이미 남아 있던 카드 2장은 DB 정리 — sjpark2, 2026-07-30
- [x] 유휴 세션 만료를 10분 → 1시간으로 연장 — 입력이 긴 작업(방문객 등록 등) 중 만료로 내용이 날아가는 것 방지. 세션 만료 동작·알려진 공백을 security.md 에 명시 — sjpark2, 2026-07-30
- [x] 직위(UT) 공통코드 기본값 추가 — UT01 사원·UT02 대리·UT03 과장·UT04 차장·UT05 부장. seed 02_seed.sql 에 반영하고 구축된 로컬 DB 에도 멱등 INSERT(NOT EXISTS)로 적용 — sjpark2, 2026-07-30
- [x] 얼굴 변환 실패 사유 노출 — 비-200 응답 본문의 Response.message/code 를 우선 노출·warn 로깅(기존엔 "HTTP 400" 만 노출), 사진 사전검증(형식·용량, 선두 바이트 판정), 템플릿 2종 모두 비면 실패 처리(조용한 실패 제거). 테스트 4건 추가 — sjpark2, 2026-07-30
- [x] 카드상태도 배지로 구분 — 정상=녹·분실=적·정지=황·반납=회색 사각점·폐기=회색 점선. 카드등록관리 목록, 정규인원·방문 카드 목록/선택 팝업(card-list.js), 기관차량등록 카드 목록까지 같은 배지 적용 — sjpark2, 2026-07-30
- [x] 목록 상태 표시를 배지로 구분 — 임시·장기출입 방문상태(신청/입실 중/퇴실 완료/신청 취소), 카드등록관리 발급대상 미발급, 기관차량등록 등록차량·사용유무·차량 상세(관리자·출입구역·카드). 색만이 아니라 점 모양·테두리도 달라 색약에서도 구분됨. 마크업은 공용 `js/core/badge.js` — sjpark2, 2026-07-30
- [x] 로그인 화면 정돈 — 흰 배경 위 흰 카드로 허전했던 것 개선: KAC 로고·시스템명·영문 부제 브랜딩 헤더, 옅은 배경(키오스크와 같은 톤, min-height 100vh로 전체 덮음), 카드 380px·패딩 조정, 입력 42px·로그인 버튼 44px로 확대, 하단 계정 문의 푸터. 색·라운드는 역할 토큰만 사용(design.md) — sjpark2, 2026-07-30
- [x] 키오스크 방문신청 화면 정돈 — 시작 화면이 문구+버튼만이라 허전했던 것 개선: KAC 로고 헤더·앰블럼, 3단계 안내(방문정보→인솔자·구역→방문객·차량), 큰 시작 버튼, 개인정보 이용 안내, 푸터 추가. 폼 카드 제목에 단계 번호 배지(CSS 카운터), 인솔자·방문구역 카드에 안내 문구. 옅은 배경(kiosk-body)으로 흰 카드 대비 강화, 768px 이하 1컬럼. 색·라운드는 역할 토큰만 사용(design.md 준수, 기존 폴백 HEX 제거) — sjpark2, 2026-07-30
- [x] 확인 모달 Enter 로 처리 — confirmModal 이 열릴 때 확인 버튼에 포커스를 주고, 열려 있는 동안 Enter=확인. keydown 을 캡처 단계에서 가로채(preventDefault+stopPropagation) 뒤쪽 화면의 Enter 핸들러(검색 등)와 충돌하지 않게 함. 확인 모달을 쓰는 11개 화면에 일괄 적용. docs/frontend.md 공통 동작 등록 — sjpark2, 2026-07-30
- [x] 카드도 존재 확인 후 upsert(자가치유) — 관리자가 BiostarX 에서 카드를 지우면 DB 에 stale biostar_card_id 가 남아 재저장해도 복구되지 않던 문제. 실 장비로 엔드포인트 확인(단건 GET /api/cards/{id}·POST /api/v2/cards/search 는 code 103 미지원, GET /api/cards 목록만 가능) 후 BiostarCardAdapter.registeredCardIds 추가. ensureBiostarCard 가 장비 존재를 확인해 없으면 재등록(새 id 로 갱신), 조회 실패는 예외로 전파(있는 것처럼 진행 금지). 테스트 3건 추가(총 31 그린) — sjpark2, 2026-07-30
- [x] 장비 미등록 카드 발급 시 자동 등록 — 방문객/인원에게 카드를 부여할 때 tb_card.biostar_card_id 가 비어 있으면 조용히 걸러져(toBiostarCardsOf filter) 장비에 카드가 붙지 않고도 '저장 성공'이 되던 문제. CardService.ensureBiostarCard 신설: 미등록 인원카드면 그 자리에서 POST /api/cards 등록 후 id 저장, 실패하면 사유와 함께 롤백(차량카드는 제외). 방문 저장(saveChildren)·정규인원 저장(saveCards)에 배선(actor/menuId 전달). CardBlacklistSyncTest 3건 추가(총 28 그린) — sjpark2, 2026-07-30
- [x] 키오스크 인솔자 검색결과 8행 스크롤 — 검색 결과가 한 번에 너무 길게 나오던 것을 8행만 보이고 나머지는 스크롤로 찾도록 변경. 공용 유틸 클래스 .list-scroll 신설(max-height=행수 기반 calc, thead sticky, 터치 스크롤) — 정의 없이 여러 화면이 쓰던 .matrix-wrap 에 스타일을 주면 다른 화면까지 바뀌므로 전용 클래스로 분리. 행수는 --rows 로 조정. conventions §2 등록 — sjpark2, 2026-07-30
- [x] 테스트용 대량 시드 스크립트 — sql/seed/99_test_seed.sql(기관·정규인원·방문객·차량·카드·방문 각 100건) + 99_test_seed_cleanup.sql. TST 접두/9xxx 번호로 식별해 삭제 쉽고, 인원ID·기관코드에 문자를 넣어 숫자형 채번·IS채번에 영향 없음. 성명·대표자는 ARIA 암호문 사전계산(결정적)해 삽입 — 화면 복호화·완전일치 검색 정상 동작 확인. BiostarX 미등록(DB 전용, 조회 테스트용). 개발DB 적용 후 목록·검색(성명/카드번호/차량번호/방문객명/인솔자명) 실측 검증 — sjpark2, 2026-07-30
- [x] 얼굴 템플릿 매핑·정규화 수정 — upload_picture 응답의 image_template=bin_type 5 / image_template_2=9 인데 코드가 반대로 매핑해 인증 템플릿이 뒤바뀌어 등록되던 문제 교정. 응답 템플릿이 고정 버퍼라 뒤가 널(0x00)로 채워져 오는 것도 잘라 표준 base64(== 패딩)로 정규화(normalizeTemplate) — 실제 응답 데이터(552→544바이트)로 검증. JSON \/ 이스케이프는 파싱 단계에서 해제됨을 테스트로 확인. BiostarFaceTemplateTest 2건 추가(총 25 그린) — sjpark2, 2026-07-29
- [x] 카드 블랙리스트 중복 해제 500 오류 수정 — 정상 카드를 저장할 때마다 removeBlacklist 를 호출해(상태 변화 무관) 이미 해제된 카드에 HTTP 500 → 직전 '실패=롤백' 정책과 겹쳐 저장이 계속 취소되던 회귀. (1) 차단 여부가 실제 바뀔 때만 호출(변경 전 상태 비교, 신규는 비차단 간주), (2) 실패 정책 비대칭화 — 차단 실패=롤백 유지, 해제 실패=경고만(보안 위험 아님·멱등성 문제). CardBlacklistSyncTest 3건 추가(총 23 그린) — sjpark2, 2026-07-29
- [x] 서비스 분리 리팩터 + userExists 3상(리뷰 🔴/🟡 후속) — spotless↔500줄 제한 상충 해소: PersonBiostarService(인원 BiostarX 동기화 전담)·VisitRosterService(방문 로스터 저장 전담) 분리, 전체 spotlessApply 적용(드리프트 38파일 해소, spotlessCheck+code-lint+테스트 동시 그린). userExists 통신오류를 BiostarSessionException 으로 전파해 퇴실·삭제 strict 경로가 장비 전면 장애에서도 롤백 — sjpark2, 2026-07-29
- [x] BiostarX 정합성 강화(감사 후속) — (1) 퇴실·인원삭제·방문삭제도 등록처럼 실패=롤백+사유 예외(장비 유령/이중사용 방지), 실패는 AuditService.logAlways(REQUIRES_NEW)로 롤백돼도 tb_system_log 에 기록. (2) 카드 블랙리스트 동기화 실패=롤백+감사(분실카드 장비 유효 방지). (3) 기관 그룹 미연동 상태로 수정 저장 시 안내 경고(자동생성 안 함). (4) 채번 레이스: 방문객 IS채번은 1회 재채번-재시도, 인원/기관/카드번호 PK 충돌은 친화 메시지로 변환. (5) 입실중(VS03)은 카드 교환만 허용(회수·방문객 제외는 퇴실로만). VisitServiceStrictTest 2건 추가(총 20 그린) — sjpark2, 2026-07-29
- [x] 카드 인쇄를 클라이언트 브라우저 인쇄로 전환 — 서버 Java(PrinterJob)로는 드라이버 여백(오른쪽/아래 흰줄)·래스터화 붉은점을 못 없앰. 참조 카드SW처럼 브라우저 window.print(@page margin:0 + object-fit:fill)로 전환(프린터가 클라이언트 PC). 서버는 앞/뒤 PNG 렌더+감사만, 일괄은 대상 전원 이미지 반환. CardPrintAdapter는 템플릿 로더로 축소, 서버 인쇄 설정(프린터명/오프셋/배율) 제거. #cardPrintArea + @media print CSS 추가 — sjpark2, 2026-07-29
- [x] 카드 인쇄 아티팩트·여백 수정 — (1) 인쇄만 붉은 점/깨짐(미리보기 정상): 렌더 이미지 ARGB를 인쇄 전 불투명 RGB로 평탄화(드라이버 알파 아티팩트 제거)+보간 BILINEAR. (2) 오른쪽/아래 안 채워짐: 인쇄가능영역→전체 용지(getWidth/Height) 기준 stretch-fill로 변경 — sjpark2, 2026-07-29
- [x] 카드 인쇄 꽉 채움(stretch-fill) — 기존 비율유지 맞춤(min-fit)+중앙배치라 오른쪽/아래 흰 여백 발생. 인쇄영역을 가로·세로 모두 채우도록 변경(카드 규격 디자인이라 왜곡 무시 수준). scale(풀블리드)·offset(위치)와 병행 — sjpark2, 2026-07-29
- [x] 카드 인쇄 배율 보정 옵션 — 프린터 여백 등으로 출력이 템플릿보다 작게 나올 때 card-print.scale(기본 1.0)로 확대(중앙 기준, min-fit 스케일에 곱). 재빌드 없이 프린터별 조정 — sjpark2, 2026-07-29
- [x] 카드 인쇄 위치 보정 옵션 — 프린터 원점 오차로 카드가 왼쪽 위로 쏠릴 때 보정. card-print.offset-x-mm/offset-y-mm(기본 0, 양수=오른쪽/아래) 추가, CardPrintAdapter.printable 의 중앙배치 좌표에 mm→pt 더함. 재빌드 없이 프린터별 조정 — sjpark2, 2026-07-29
- [x] 방문객 마지막 사용 카드 기록 — 카드가 퇴실 시 회수(person_id=NULL)·재사용돼 추적 불가하던 문제. tb_visit_person.last_card_no(스냅샷) 추가, 카드 배정 시 기록(saveChildren→updateVisitorLastCard, 카드번호=biostar_card_value). 회수 후에도 보존, 최종값 1건. detail 이 lastCardNo 반환→방문객 탭 카드칸에 '회수됨(번호)' 표시 — sjpark2, 2026-07-29
- [x] 퇴실완료 방문 읽기전용 처리 — 임시·장기 방문 수정 모달에서 퇴실완료(VS04)면 입력·버튼을 비활성화(회색·클릭불가, 탭 전환은 허용)하고 제목을 '방문 상세 (퇴실완료 — 수정 불가)'로 표시. 기존엔 저장 버튼만 숨겨 헷갈렸던 것 개선. .visit-modal.readonly CSS + visitor.js 토글 — sjpark2, 2026-07-28
- [x] 정규인원 수정도 BiostarX 동기화 필수화 — 등록에 이어 수정(update)도 BiostarX 동기화 성공해야 커밋(실패=기관 그룹 없음·장비오류면 롤백+사유 예외). 등록/수정 공통 헬퍼 syncPersonToBiostar(form, before)로 통합(등록=before empty, 수정=변경전 스냅샷). 장비-DB 정합성 최우선. docs/integration.md 정책 갱신 — sjpark2, 2026-07-28
- [x] 기관코드 자동 채번 + 정규인원 등록 BiostarX 필수화 — (1) 기관 등록 모달에서 기관코드를 다음 번호로 자동 채움(TbCompanyMapper.selectNextCompanyCode=숫자형 max+1, CompanyController /nextCode, company.js). (2) 정규인원 등록은 BiostarX 사용자 생성이 성공해야 커밋 — 실패(설정/기관 biostar_group_id 없음·장비 오류)면 트랜잭션 롤백 + 사유 예외(유령 인원 방지). 그룹 없으면 BiostarX 호출 전 차단하고 "기관 먼저 동기화" 안내. 수정은 기존 정책 유지 — sjpark2, 2026-07-28
- [x] 검색조건 개편(임시·장기·정규·기관차량) — 임시/장기: 방문유형 검색 제거, keyword 로 방문객명·인솔자명(ARIA 완전일치)·차량번호·카드번호(부분) 조회(EXISTS 서브쿼리). 정규인원: 주요업무 검색 제거, 성명(완전일치)·카드번호(부분) 추가. 기관차량: 검색어에 소속 차량번호 추가(searchCar 플래그로 기관등록관리와 격리). PageParam.keywordEnc(성명 암호화 완전일치) 신설 — sjpark2, 2026-07-28
- [x] form-hint 12px + 엑셀 안내문 항목별 목록화 — 전역 .form-hint 폰트 11→12px. 엑셀 등록 모달 안내(hint)를 문자열→배열로 받아 .hint-list 불릿 목록(들여쓰기·줄바꿈)으로 렌더(excel-import 컴포넌트/모달 div화). 카드·정규인원·기관 화면 hint 배열화 — sjpark2, 2026-07-27
- [x] 엑셀 양식에 예시 행 추가 — 카드·정규인원·기관 양식 다운로드 시 2행에 예시 데이터를 넣어 입력 형식을 안내. 예시 행(EXAMPLE_ROW)은 임포트 시 Arrays.equals 로 걸러 건너뜀(사용자가 덮어쓰거나 지우면 정상 등록). ExcelUtil.download 는 List.<String[]>of(EXAMPLE_ROW) 로 호출 — sjpark2, 2026-07-27
- [x] 카드 발급 시 출입구역 선택 강제(프론트 가드) — 카드를 발급했는데 해당 출입구역이 없으면 실제로 못 여는 무효 카드가 되므로, 저장/발급 전 토스트로 차단. 임시·장기(visitor): 방문객 카드→인원 출입구역, 차량 카드→차량 출입구역. 정규인원(person): 카드→출입구역. 기관차량등록(companyCar): 카드 발급 전 차량 출입구역 지정 강제. 프론트 검증만(백엔드 미변경) — sjpark2, 2026-07-27
- [x] 카드 엑셀 일괄등록 — 카드등록관리에 '엑셀 등록' 추가. 양식 다운로드(/card/card/excel/template)+업로드(/excel/import). 공용 excel-import 모달/컴포넌트·ExcelUtil·ExcelImportResult 재사용, CardImportService가 행마다 CardService.createCard(프록시=독립 트랜잭션) 호출→중복/필수/BiostarX 검증 재사용, 실패 행은 사유와 함께 요약. 카드상태 비면 CS01 기본, 미발급으로 등록 — sjpark2, 2026-07-27
- [x] 인솔자 겹침 규칙 — 임시(PT02)끼리는 같은 인솔자 중복 불가, 임시↔장기·상주(PT03/PT04) 및 장기끼리는 허용. '겹침'=진행중(신청 VS01·입실중 VS03) 판정(퇴실완료 VS04·취소 VS02·삭제 제외). 등록 대상이 임시일 때만 검사(create/update/키오스크 저장 전). TbVisitMapper.selectActiveTempManagers + VisitService.checkManagerOverlap — sjpark2, 2026-07-27
- [x] 장기출입등록 메뉴(102) — 임시인원관리 하위 /visitor/longterm. 임시인원등록(101)과 동일 구성이나 방문유형을 PTD03 계열(장기 PT03·상주 PT04) select 로 선택, 목록은 PTD03 계열만. 화면(web/visitor/visitor)·VisitService 공유, 차이는 window.VISIT_CFG 서버주입. LongTermController 신규. seed 메뉴/권한 + 개발DB 반영 — sjpark2, 2026-07-27
- [x] 키오스크(무인증) 방문 신청 — /kiosk/visit 공개 화면. 방문그룹정보·인솔자(정규인원 검색)·방문구역(인원/차량 2단)·방문객·차량 입력→임시(PT02)·신청(VS01) 저장(카드/BiostarX 없음, 관리자 카드발급 단계로 위임). KioskController/KioskVisitService. 리뷰 2026-07-27 — sjpark2, 2026-07-27
- [x] 정규인원 카드 프린트 — 카드정보 탭 프린트 버튼(미리보기 후 인쇄), 얼굴+카드 등록 인원만. card_project(디자인 export) JSON을 배경 해상도로 스케일 렌더(앞/뒤), 한글 폰트 폴백. adapter/cardprint(CardPrintAdapter/Renderer)+CardPrintService — sjpark2, 2026-07-27
- [x] 임시인원등록(방문, tb_visit) — 임시인원관리(100)/임시인원등록(101), /visitor/visitor. 4탭(그룹정보·인솔자·방문객·차량): 사용자출입그룹(tb_ac_group)·차량출입그룹(CAR), 인솔자(정규인원), 방문객(tb_person+affiliation 신규컬럼)·차량(tb_car)+카드선택. 저장 시 방문객 BiostarX PT→PTD code_tag 부모그룹 편입(VisitBiostarService). 카드·출입그룹은 정규와 동일 테이블 재사용 — sjpark2, 2026-07-24
- [x] 정규인원 엑셀 일괄등록 + 엑셀등록 공용 컴포넌트화 — 정규인원등록에 엑셀 등록(PersonImportService, 행별 create 재사용·독립 트랜잭션, 기관코드·성명 필수, 인원ID 자동채번, 사용자권한·카드정보 제외). 기관/인원이 공유하는 excel-import 컴포넌트 신설(기관 화면도 이행) — sjpark2, 2026-07-23
- [x] 기관 엑셀 일괄등록 — 양식 다운로드(빈 헤더) + 엑셀 업로드(행별 create 재사용, 행 단위 독립 트랜잭션, 기관코드·기관명 필수, 성공/실패 요약). ExcelUtil.read 파싱 추가, multipart 상한 5MB — sjpark2, 2026-07-23
- [x] 정규인원 등록 편의·차량카드 정책 — ①등록 시 인원ID 자동 채번(숫자형 PT01 최대+1, GET /person/person/nextId) ②차량 카드(CDT02)는 BiostarX 미등록으로 확정(issueCard·createCard 분기, tb_card 만 저장) ③기관 선택 팝업 검색 입력 폭 확대 — sjpark2, 2026-07-23
- [x] 정규인원 ↔ BiostarX 사용자 upsert — 저장 전 GET /api/users/{인원ID} 로 존재를 확인해 있으면 PUT, 없으면 POST. 등록·수정 어느 쪽에서 들어와도 결과가 같아져 DB 와 장비의 어긋남이 저장 한 번으로 맞춰진다 — sjpark2, 2026-07-22
- [x] 기관차량등록 — 차량관리자(해당 기관 정규인원 선택)·차량 불러오기(미할당 차량 편입)·출입구역(신규 tb_car_ac_group, tb_common CAR). car_manager_id 의미를 tb_login_user → tb_person(정규인원)으로 확정 — sjpark2, 2026-07-22
- [x] 기관차량등록을 기관 중심으로 재구성 — 목록을 차량 → **기관**(tb_company del_yn='N', 등록차량 수)으로 바꾸고, 기관을 누르면 모달에서 차량 목록 → 우측 패널(차량 정보 + 카드 발급/회수). 차량 자체 관리는 차량등록관리 담당 — sjpark2, 2026-07-22
- [x] 기관차량등록(/company/companyCar) — 기관관리(700) 하위 702. 기관 소속 차량(tb_car + company_code)과 차량용 카드(tb_card + car_id) 발급/회수. 카드구분은 서버가 차량 고정·패스구분 미사용, 카드 있는 차량은 삭제 거부, 회수는 car_id=NULL(재사용). 곁들여 잘못된 JSON 본문이 500 → 400 이 되도록 GlobalExceptionHandler 보강 — sjpark2, 2026-07-22
- [x] 카드등록관리 검색조건 카드구분(CDT) 추가 — 검색조건 코드팝업 3종(카드구분·카드상태·패스구분) 바인딩을 표 기반 반복문으로 통합 — sjpark2, 2026-07-22
- [x] 카드등록관리 보완 — 검색조건에 패스구분(PT) 추가, 할당인원 라벨 힌트 제거, 카드구분=차량(CDT02)이면 패스구분 비활성·필수 제외(클라 잠금 + 서버 validate/normalize 로 값도 비운다) — sjpark2, 2026-07-22
- [x] 카드등록관리(tb_card) — 카드관리(800)/카드등록관리(801), /card/card 수직 슬라이스. 목록(카드상태·할당여부 필터, 정렬)·등록(BiostarX 카드 생성 성공해야 저장)·수정(카드번호·할당인원 불변)·삭제(소프트, 할당된 카드는 거부). 발견한 버그 동반 수정: 할당하기로 고른 회수 카드가 인원 저장 시 새 행으로 복제되던 문제 + 카드번호 필터 유니크 인덱스(UX_tb_card_value) 추가 — sjpark2, 2026-07-22
- [x] 카드 할당하기 팝업 — 미할당(회수) 카드 목록(GET /card/unassigned, 카드번호·명칭 검색)에서 골라 카드번호·패스구분·명칭·상태를 채운다. 카드번호 입력칸 축소(w-160) + SCAN 오른쪽에 배치. 폭 유틸(.w-*)이 `.form-group .input{width:100%}` 에 밀리던 것도 함께 수정 — sjpark2, 2026-07-22
- [x] 카드 회수·재사용 — 목록에서 '제외'를 소프트삭제(del_yn='Y') → **회수**(person_id=NULL, use_yn='Y', del_yn='N')로 변경해 다른 인원이 같은 실물 카드를 다시 쓸 수 있게. 등록 시 tb_card 에 같은 카드번호가 있으면 POST /api/cards 를 부르지 않고 재사용하고, **타인에게 발급된 번호는 거부**(한 카드 두 사람 부여 차단). 개발DB의 구 삭제 카드 1건 이행 — sjpark2, 2026-07-22
- [x] 카드 입력 필수값·표기 정리 — 카드번호·카드구분·패스구분·카드명칭·카드상태 필수(클라 토스트 + CardService 서버 검증, 실패 시 인원 저장 트랜잭션 롤백이라 BiostarX 사용자 생성 전에 막힘). SCAN 문구, 목록 컬럼에서 발급료 납부일 제외 — sjpark2, 2026-07-22
- [x] 카드 패스구분·우측 패널 — tb_card.pass_type 추가(tb_common PT). 카드 입력을 별도 오버레이 → 편집 모달 오버레이 안의 형제 컨테이너로 바꿔 **우측에 나란히** 열리게(z-index 다툼 제거, 좁은 화면은 아래로 접힘). 카드 목록 행 클릭 시 같은 패널에 값 표시·수정(카드번호는 읽기전용). 카드종류는 CardService 가 CDT01(인원) 고정 — sjpark2, 2026-07-22
- [x] 카드정보(tb_card) — 인원 모달 카드정보 탭: 카드 추가(카드번호 직접입력 또는 장치 scan_card) 시 BiostarX 카드 즉시 등록(POST /api/cards, CSN 고정) → id/card_id 를 biostar_card_id/biostar_card_value 로 보관, 인원 저장 시 tb_card 저장 + 사용자 payload cards[] 부여(수정은 델타). 공용 컴포넌트 card-list 신설, 얼굴 중계는 PersonFaceService 로 분리(PersonService 503→477줄), CS/IS 공통코드 시드 — sjpark2, 2026-07-22
- [x] 정규인원 입력 포맷 고정 — 인원ID 영문·숫자만(입력 즉시 필터 + 서버 정규식), 생년월일 YYYY-MM-DD 자동 하이픈 + 실재 날짜 검증(클라·서버 동일 기준). 한계선 정리 동반: PersonService 폼→행 매핑 toRow() 통합·BiostarX 요청 오버로드(506→489줄), 출입권한 트리를 공용 컴포넌트 ac-group-tree 로 분리(person.js 416→372줄) — sjpark2, 2026-07-22
- [x] 정규인원 목록·삭제 UX 및 증빙문서 첨부 — 목록 컬럼 재구성(선택/인원ID/성명/기관코드/기관/직위/생년월일/출입기간, 행별 관리 컬럼 제거), 체크박스 다중선택 + 툴바 '선택 삭제'(DELETE /bulk, 건별 BiostarX 동기화·실패만 경고), 수정 모달 좌측 삭제 버튼, 시스템 등록일(reg_dt) 읽기전용 표시. 회보/승인 근거문서는 경로 텍스트 → **첨부파일**(tb_person_file, 파일 실체 DB 보관, 종류당 1건, 5MB, 다운로드는 attachment) + 공용 컴포넌트 file-field 신설 — sjpark2, 2026-07-22
- [x] 인원·카드·출입그룹 스키마 설계 — tb_person(인원, 성명·생년월일·연락처 ARIA, 발급유형 person_type=PT, tb_login_user 와 이름 분리해 조인 오용 차단), tb_person_photo(등록사진 BASE64 1:1 분리 — 목록 성능·생체정보 노출면 축소), tb_card(인원 1:N, card_status 단일화로 상태 모순 차단), tb_person_ac_group(출입권한 N:M, 복합PK 로 중복부여 차단). security.md Enc=Y 목록 7컬럼 갱신 + 비암호화 결정(카드값·사진) 근거 기록 + 출입권한 부여/회수 감사 필수화. DDL·문서만(코드 없음), 공통코드 시드 UT/PT/CDT/CS/IS 는 후속 — sjpark2, 2026-07-21
- [x] 정규인원등록(tb_person) — 정규인원관리(200)/정규인원등록(201), /person/person 수직 슬라이스. person_type=PT01 고정, 성명·생년월일·연락처 ARIA, 탭 모달(사용자정보/사용자권한/카드정보), 출입권한은 tb_ac_group 트리 선택→tb_person_ac_group, 얼굴은 파일업로드(upload_picture)/장치촬영(credentials/face) 서버 중계 + tb_person_photo, 등록 시 BiostarX 사용자 생성(POST /api/users, 실패는 경고). 수정/삭제·카드는 추후 — yg-kwak, 2026-07-15
- [x] 기관 ↔ BiostarX 사용자그룹 연동 — tb_company.biostar_group_id 추가, PT→PTD01 code_tag(부모 그룹ID) 체인, 등록 시 선택(모달)/미선택 시 생성(POST /api/user_groups), 기관명 변경 시 그룹명 수정(PUT), 선택 팝업은 user_groups/search 를 부모로 필터. 연동 실패는 기관 저장 유지 + 경고. PT/PTD seed — yg-kwak, 2026-07-15
- [x] 기관등록관리(tb_company) — 기관관리(700)/기관등록관리(701), /company/company 수직 슬라이스. PK=company_code 중복방지, 대표자(ceo_name) ARIA 암호화, 기관구분=공통코드 CO 코드팝업, 용역시작/종료일, use_yn 활성 + del_yn 소프트삭제. CO 코드 seed·building 아이콘 — yg-kwak, 2026-07-15
- [x] 차량등록관리(tb_car) — 차량관리(600)/차량등록관리(601) 메뉴, /carInfo/car 수직 슬라이스(모델·매퍼·서비스·컨트롤러·html·js). 차량번호 중복 방지(del_yn='N' 기준), 차종=공통코드 CT 코드팝업, 소프트 삭제(del_yn), 관리자ID 추후. CT 코드 seed·사이드바 car 아이콘·code-lint [4] 영역 일반화 — yg-kwak, 2026-07-15
- [x] 보안 영역 분리 — 보안관리 menu_id 500번대(500/501/502), URL·뷰·JS 를 /system → /security 로 이동(web/security/), 컨트롤러 매핑·seed·권한·감사로그 menu_id 재지정. code-lint [4] security 영역 포함 — yg-kwak, 2026-07-14
- [x] 메뉴 구성 재편 — 보안관리(guard) 그룹 신설, 감사추적·출입권한관리를 그 아래로 이동, 시스템관리 하위를 설정/공통코드/권한메뉴/사용자관리 순으로 재배치(tb_menu seed) — yg-kwak, 2026-07-14
- [x] 헤더 계정 메뉴(자가서비스) — 사용자명 드롭다운(시작메뉴 변경/비밀번호 변경/로그아웃), 시작메뉴는 tb_login_user.start_menu_id 저장→다음 로그인 시 해당 화면 진입, 비밀번호는 이전값 확인 후 변경. AccountController(/account, 로그인만) + core/header-user.js — yg-kwak, 2026-07-14
- [x] 출입권한관리(tb_ac_group) — tb_common(AR) 최상위 동기화(진입 시 insert/delete, ar_code 컬럼 추가), 트리, BiostarX 출입그룹(access_groups/search) 하위 매핑 추가/수정/삭제(최상위 삭제불가) — yg-kwak, 2026-07-14
- [x] 공통코드관리: 구분 컬럼(N=시스템/Y=사용자) + 전체 노출, 시스템 코드는 삭제불가·이름/사용유무만 수정, 등록 모달 코드구분ID 전체 노출 — yg-kwak, 2026-07-13
- [x] 하네스 점검 반영(OpenAI 원칙 대조): 강제공백 메움(code-lint에 MENU_ID금지·수동페이징금지·@RequestMapping↔menu_url), AGENTS §4 메뉴접근통제 불변식, new-screen 갱신, UI규칙 conventions→frontend 이관, 순수로직 단위테스트(MenuService/MenuNode)+smoke 격리 — yg-kwak, 2026-07-13
- [x] menu_id 하드코딩 제거 — 요청 URL(=menu_url) 서버 역조회로 menu_id 결정(MenuAccessInterceptor+CurrentMenu). 컨트롤러 MENU_ID 상수 삭제, 메뉴 접속(MENU) 감사 자동화(누락분 해결) — yg-kwak, 2026-07-13
- [x] 공통화: 윈도우형 페이징(core/pager.js)·기간 프리셋(core/period.js)을 core 컴포넌트로 추출, 전 목록화면(공통코드·사용자·감사추적) 적용 + conventions/frontend 규칙 등록 — yg-kwak, 2026-07-13
- [x] 감사추적(tb_system_log) 조회 전용 페이지 — 기간·유형·검색어 필터, 유형/메뉴 조인 표시, 엑셀(목적 감사), 최신순 기본 정렬. 조회/엑셀도 자체 감사 기록 — yg-kwak, 2026-07-13
- [x] 권한메뉴관리 트리에 메뉴별 조회/입력·수정/삭제 체크 추가 — 선택=조회, 미선택 시 입력·수정/삭제 해제·비활성. smoke 가드(시드 권한 auth_id≤2 update/delete 금지) — yg-kwak, 2026-07-13
- [x] 권한메뉴관리 UX 개편 — 좌 권한목록/우 설정(전체 메뉴 트리 체크, 캐스케이드·전체선택·신규/수정완료/삭제), 화면 엑셀·페이지크기 제거. MenuNode.buildTree 공용화, MenuService.fullTree — yg-kwak, 2026-07-13
- [x] 권한메뉴관리(tb_menu_auth) CRUD — 권한 그룹 + 메뉴별 CRUD 권한 매트릭스(조회/등록·수정/삭제), 사용중 권한 삭제 가드, 엑셀 + smoke — yg-kwak, 2026-07-13
- [x] 사이드바 권한 필터(read 권한 메뉴만, root 전체·빈 그룹 숨김) + 무권한 URL 직접접근 시 403 권한없음 페이지(error/forbidden) — yg-kwak, 2026-07-13
- [x] 필수(*) 표기 확대(공통코드·설정) + picker 필드 삭제버튼 공통화 + 하네스 규칙(신규메뉴 필수*·클릭선택 삭제버튼) — yg-kwak, 2026-07-13
- [x] 사용자 폼 개선 + 공통 컴포넌트: 필수(*) 표기, 비밀번호 표시토글(전 페이지), tb_common 코드 팝업(code-picker) — 근무지역을 팝업 선택으로. 시작메뉴/관리자여부 필드 제거(권한 필수화) — yg-kwak, 2026-07-13
- [x] 명명규칙 정비: 네이밍 앵커=테이블 어간(§1 명문화). 화면/라우트/컨트롤러/서비스를 stem 으로 통일 — commonCode→common, User*/user→LoginUser*/loginUser (Mapper/Model 은 Tb{Stem} 유지) — yg-kwak, 2026-07-13
- [x] 사용자관리(tb_login_user) CRUD — 골든 패턴 전개(성명/비번 ARIA, 비번 WRITE_ONLY·수정 시 유지, 권한/시작메뉴/근무지역 참조 select, root 부여 가드·본인삭제 차단, 엑셀) + smoke 36체크 — yg-kwak, 2026-07-13
- [x] UI 테마: Notion→Ant 대비 + 차콜 다크 프레임(헤더/사이드바), 헤더 블루 강조선, 사이드바 hover 파랑·클릭 전용 플라이아웃, 입력 자동완성 전역 차단 — yg-kwak, 2026-07-13
- [x] CSS 완전 토큰화 + Notion 테마 적용(팔레트→역할 2단계, 사이드바 라이트·토스트·헤더 토큰화) — yg-kwak, 2026-07-10
- [x] 사이드바 계층형 개편(그룹→하위, 접기/펼치기, 접힘 시 아이콘 플라이아웃, menu_icon) — yg-kwak, 2026-07-10, `54fd471`
- [x] 설정관리(tb_system) — BiostarX 연동정보 저장/연결테스트(adapter, pw ARIA 암호화) — yg-kwak, 2026-07-10, `b583328`
- [x] 공통 토스트 알림(success/error/warning, 서버 return 문구 자동) — yg-kwak, 2026-07-10, `22c8a8a`
- [x] 능동 작업보드(TASKS.md) 제거, 완료 기록만 유지 — yg-kwak, 2026-07-10, `f051ff0`
- [x] 공통코드 시스템코드(N) 조회 제외 + 구분 컬럼 제거 — yg-kwak, 2026-07-10, `36f2069`
- [x] 공통코드 코드구분 select(허용 구분만) + 코드구분명 자동 — yg-kwak, 2026-07-10, `4e1f6d9`
- [x] 공통코드 시스템코드(N) 화면 CRUD 차단 — yg-kwak, 2026-07-10, `41ab35e`
- [x] 하네스 자동화 보강(원문 격차 1~6: 스크립트/CI린트/ArchUnit/드리프트/MDC) — yg-kwak(+Claude), 2026-07-10, `0fa23fa` `c6860f5`
- [x] 리뷰 기록(docs/reviews) 도입 — yg-kwak, 2026-07-10, `6f87433`
- [x] 규칙 대장(conventions.md) 전면 확장 + 자동 등록 장치 — yg-kwak, 2026-07-10, `c7d7e23`
- [x] 조회 감사(READ 조건+건수) + LOGIN/LOGOUT + user_input 정정 — yg-kwak, 2026-07-10, `11b9ea3`
- [x] 엑셀 다운로드(목적→감사 remark, 전체 데이터) — yg-kwak, 2026-07-10, `a02b72d`
- [x] 행 클릭 수정 + 공통 확인/입력 모달(components) — yg-kwak, 2026-07-10, `0d10e50` `a02b72d`
- [x] 메뉴 권한 기반 CRUD 통제(화면+서버 이중 방어, viewer 계정) — yg-kwak, 2026-07-10, `2698234`
- [x] 목록 강화(정렬/페이지크기/검색조건/사용여부) + 레이아웃 — yg-kwak, 2026-07-10, `bc73d08` `419f2d9`
- [x] Gradle 저장소 루트 이동(project/ 제거) + IntelliJ 실행 정리 — yg-kwak, 2026-07-10, `3207b97`
- [x] 골든 샘플: 공통코드관리 CRUD — yg-kwak, 2026-07-09, `5747902`
- [x] 앱 스캐폴딩 + DB(DDL/seed) + 공통기반(인증/ARIA/감사/예외) — yg-kwak, 2026-07-09, `5747902`
- [x] 하네스 구축(AGENTS/docs/skills/hooks/CI) 및 정합화 — yg-kwak, ~2026-07-08, `1592fe3` 외
