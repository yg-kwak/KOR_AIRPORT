# 리뷰: 정리(doc-gardening) — 죽은 mapper 삭제 · 어긋난 주석·문서 정정

- 일시: 2026-08-18 / 검토자: sjpark2
- 대상: `main` d965077 이후 **정리 diff**(5 files, +7 −22)
- 결과: 🔴 0 · 🟡 1 · 🟢 4

`/cleanup` 실행 결과를 검토한다. 정리는 **삭제**를 포함하므로 경계 검토(`/review`)만으로는 부족하고,
동작 검증(test·smoke)을 먼저 돌린 뒤 이 문서를 쓴다.

## 기계 검증 (정리 전)
`docs-lint` · `schema-drift`(23테이블 전 컬럼) · `code-lint`(7항목) 모두 통과 — 기계가 잡는 드리프트는 없었다.
이번에 나온 것은 전부 **의미 수준**이다.

## 🔴 차단
없음.

- 계층·MyBatis·외부격리·ARIA·감사: 이번 diff 는 **삭제와 주석뿐**이라 경계에 손대지 않았다.
- 비밀값 추가 없음, JPA 없음, Service/Controller 인라인 SQL 없음.

## 정리한 것

### 1. 죽은 mapper 삭제 — `TbCarAcGroupMapper.selectCodeNames`
[TbCarAcGroupMapper.java](../../src/main/java/AirPort/mapper/TbCarAcGroupMapper.java) 선언 + [XML](../../src/main/resources/mapper/TbCarAcGroupMapper.xml) select

- 전수 검색(java·xml·js·html·sql·scripts)에서 **정의 2곳 외 참조 0**.
- 같은 로직이 [TbCarMapper.xml:109](../../src/main/resources/mapper/TbCarMapper.xml) 에 인라인으로 있고 **그쪽이 실제로 쓰인다**(`ac_code_names` → `companyCar.js:107`). 중복 구현이라 한쪽만 고치면 조용히 어긋난다.
- **2026-08-04 리뷰가 이미 지적한 항목**이다. 함께 지적된 `updateLoginFailCnt` 는 그 뒤 계정잠금 기능이 붙어 살아났고, 이것만 2주째 남아 있었다.
- 주석이 담고 있던 MSSQL 2016 제약(STRING_AGG → FOR XML PATH)은 **살아 있는 쿼리의 주석**과 [database.md MSSQL 규약 표](../database.md)에 그대로 있어 지식이 사라지지 않는다.

### 2. 해소된 TODO + 사실과 다른 주석 — `TbCar.carManagerId`
`→ tb_login_user.user_id (TODO: 관리자ID 화면 추후)` 였다. 둘 다 틀렸다 —
화면은 이미 있고(기관차량등록의 관리자 선택 팝업), 실제 조인 대상은 `tb_person` 이다
([TbCarMapper.xml:113](../../src/main/resources/mapper/TbCarMapper.xml), `CompanyCarService.managersOf` → `personMapper.selectByCompany`).

### 3. 같은 문서 안의 모순 — `database.md`
컬럼표(168행)는 `tb_person.person_id`, 관계 요약(419행)은 `tb_login_user.user_id` 라고 적고 있었다.
코드가 맞는 쪽으로 통일했다.

### 4. 깨진 마크다운 표 5행
- `database.md` 3행(`tb_car.car_manager_id`·`company_code`, `tb_card.car_id`) — **Enc 칸이 빠져** 설명이 Enc 열에, 비고가 설명 열에 렌더됐다.
- `conventions.md` 2행 — 코드스팬 안의 `|`(`badge-info|success|…`, `openModal('create'|'edit')`)가 칸 구분자로 먹혀 표가 밀렸다. GFM 은 코드스팬 안이라도 `\|` 로 escape 해야 한다.

`docs-lint` 는 링크만 보므로 이 표 깨짐은 잡지 못한다.

## 🟡 권고

### 🟡-1 표 칸 수 검사를 docs-lint 에 넣을 만하다
이번 5행은 **사람이 세어서** 찾았다. 헤더 대비 칸 수 비교는 기계가 할 일이고(escape 된 `\|` 만 빼면 됨),
표가 많은 저장소라 또 생긴다. `scripts/docs-lint.sh` 에 한 항목 추가를 검토한다.

## 🟢 양호
- **죽은 코드가 2건뿐이었고 그중 1건은 스스로 살아났다** — 전반적인 코드 위생은 좋다. 미참조 클래스 0, 미호출 서비스 메서드 0, 죽은 JS 함수 0, 미사용 CSS 클래스 0.
- **삭제 회귀를 실제로 검증했다** — 테스트 224건 + **스모크 185/0**. 스모크에는 삭제한 mapper 를 타던 화면(기관차량 조회·등록·카드발급)이 포함된다. `/review` 는 read-only 라 이런 회귀를 못 잡는다.
- **이력 문서를 고치지 않았다** — `tasks-archive.md`·`docs/reviews/*` 는 그 시점 사실이라 최신 동작과 달라도 그대로 둔다. 고치면 이력이 왜곡된다.
- **삭제 전에 지식의 행방을 확인했다** — 지운 주석의 MSSQL 제약 설명이 살아 있는 쿼리와 database.md 에 남아 있음을 보고 지웠다.

## 조치
- 정리 4건: 반영(이 커밋).
- 🟡-1: 미조치. `docs-lint` 확장은 별도 작업으로 판단.
