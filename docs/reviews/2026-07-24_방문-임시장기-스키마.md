# 리뷰: 방문(임시·장기) 스키마 설계 (tb_visit + 5개 매핑, VS 코드)
- 일시: 2026-07-24 / 검토자: SUPREMA\sjpark2
- 대상: main 72d5b16 + 워킹트리(미커밋) — `sql/ddl/01_tables.sql`, `sql/seed/02_seed.sql`, `docs/database.md`, `docs/integration.md`
- 결과: 🔴 0 · 🟡 6 · 🟢 8

변경 성격: **DDL + 시드 + 문서만**. Java/Mapper/화면 코드 없음 → 계층경계·MyBatis·adapter·암호화·감사 항목은 이번 대상 아님. 대신 설계가 향후 불변식 위반을 유발하지 않는지 관점으로 검토.

## 🔴 차단
없음.

## 🟡 권고
- [sql/seed/02_seed.sql] **`CAR` 공통코드 미시드**: `tb_visit_car_ac_group`(신규)·`tb_car_ac_group`(기존)이 `tb_common`(cmm_id='CAR')를 참조하나 시드에 CAR 행이 없다(AR만 존재). 차량 구역 선택이 신규 환경에서 빈다 → CAR 시드 추가 또는 "런타임 공통코드관리로만 관리"임을 문서에 명시. (기존 테이블도 걸린 선행 갭)
- [sql/seed/02_seed.sql:49] **`code_remark` 의미 과부하**: PT.`code_remark` 를 '메모'가 아니라 동작 플래그('Y'=하위트리 노출)로 재사용한다. 공통코드관리 화면이 이 값을 메모로 노출·편집하므로 관리자가 무심코 바꾸면 방문 구역 선택 로직이 깨진다. 규칙은 문서화됐으나, 화면에서 보호(읽기전용/설명 표기)하거나 전용 의미임을 UI 에 표시 권장.
- [sql/ddl/01_tables.sql] **`tb_visit.status_code` 초기값 미정**: 신규 방문의 기본 상태(VS01 신청)가 DDL 기본값·문서에 없다. 서비스에서 세팅하거나 문서에 초기 상태 규칙 명시.
- [sql/ddl/01_tables.sql] **`tb_visit_manager` 한 visit 내 동일인 중복 허용**: PK `(visit_no, seq)` 는 같은 `person_id` 를 순번만 달리해 두 번 넣을 수 있다. "임시는 인솔자 중복 금지"뿐 아니라 한 visit 내 중복도 서비스에서 막아야 한다(의도된 설계이므로 확인용).
- [sql/ddl/01_tables.sql] **`tb_visit.evidence_file` 파일 실체 미저장**: `tb_person_file`(varbinary, 고아파일 방지) 패턴과 달리 파일명 text 만 둔다(사용자 결정). 실제 파일 저장·조회 위치가 미정이라 업로드 경로·고아파일 문제가 재현될 수 있다 → 파일 저장 방식 후속 정의 필요.
- [docs/integration.md] **materialize·최상위전용은 서비스 강제 + 테스트 필요**: 승인 시 최상위→하위 확장, 최상위 노드만 선택, work 기간→access 기간 전파는 정적 제약으로 못 막는다. 구현 시 서비스 검증 + 순수로직 단위테스트 대상.

## 🟢 양호
- 비밀값·JPA·인라인 SQL 없음(코드 없음).
- **카드·인원권한·차량권한을 정규와 동일 테이블(`tb_card`/`tb_person_ac_group`/`tb_car_ac_group`)로 재사용** — 방문 전용 병렬 구조를 만들지 않아 BiostarX 연동·회수·중복방지 로직이 그대로 재사용됨(요청한 "일관 맵핑" 충족).
- 매핑 테이블에 `del_yn` 없음(하드 삭제 + 감사) — `tb_person_ac_group` 패턴과 일치. 마스터(`tb_visit`)만 소프트 삭제.
- 명명 일관: `tb_visit` 어간, `PK_`/`CHK_` 제약 네이밍, `reg_dt`/`mod_dt`, 복합 PK.
- `code_remark` 데이터 제어로 방문유형별 구역 범위를 하드코딩 없이 분기.
- 방문자 BiostarX 편입(기관 그룹 미생성, PT→PTD 부모 그룹 직접 편입)·materialize 규칙을 `integration.md` 에 명문화(어댑터 경계 설계 유지).
- 이름 충돌 회피: "user group=기관"과 겹치던 `tb_user_group` 을 `tb_visit` 로 확정.
- `VS`(방문상태) 신규 4종 + PT `code_remark` 시드 반영. 시드에 운영 조정 경고 주석 포함.
- 검증: `scripts/schema-drift.sh` ✅(테이블 20종·전 컬럼·역방향) / `scripts/docs-lint.sh` ✅.

## 조치
- 🟡-1 (CAR 시드): 미반영 — 시드 추가 여부/런타임관리 결정 필요.
- 🟡-2 (code_remark 과부하): 미반영 — 화면 보호는 구현 단계 과제.
- 🟡-3 (status 초기값): 미반영 — 서비스 세팅 or 문서 명시.
- 🟡-4 (인솔자 중복): 확인 필요 — 의도 확인 후 서비스 가드.
- 🟡-5 (evidence_file 저장방식): 미반영 — 후속 정의.
- 🟡-6 (materialize 강제/테스트): 구현 단계 과제 — 규칙은 문서화 완료.
