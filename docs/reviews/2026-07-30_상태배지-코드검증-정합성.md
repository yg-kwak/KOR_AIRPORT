# 리뷰: 상태 배지 · 공통코드 검증 · 삭제 정합성 · 화면 정돈

- 일시: 2026-07-30 / 검토자: SUPREMA\sjpark2
- 대상: main `4d4d8b7..eee8b4c` (커밋 21개, 75파일 — 얼굴 템플릿 교정, 블랙리스트 중복해제 수정, 카드 upsert 자가치유, 키오스크·로그인 화면 정돈, 상태 배지, 얼굴 실패 사유, 직위 코드, 세션 1시간, 인원 삭제 시 카드 회수, 발급대상 검색, 시스템코드 사용유무 고정, 공통코드 검증, 기관 차량 모달 폭)
- 결과: 🔴 0 · 🟡 5 · 🟢 8 → **🟡 5건 전부 반영 완료**(아래 재검토 참고)

## 🔴 차단 (불변식 위반)

없음. 이전 리뷰(2026-07-29)의 🔴(spotless ↔ code-lint 상충)는 서비스 분리로 해소되어 **재발하지 않았다** — `gradlew spotlessCheck` exit 0, `code-lint`/`docs-lint` 전 항목 ✅, 테스트 43건 전부 통과.

## 🟡 권고

- **[CardService.java:441](src/main/java/AirPort/service/CardService.java:441) 인원 화면 카드 저장 경로에 공통코드 검증이 없다 — 검증 적용이 반쪽**
  이번에 도입한 `CodeValidationService` 는 `validateCard()`(카드등록관리 create/update)에만 걸리고, 정규인원등록의 카드 탭이 쓰는 `saveCards → validate(CardForm)` 은 필수값만 본다. 화면은 코드 팝업 선택이라 실무상 잘못된 값이 오기 어렵지만, "클라이언트 값 불신"(conventions §5) 관점에서는 같은 저장 대상에 규칙이 다르게 걸린 상태다.
  → `validate(CardForm)` 을 인스턴스 메서드로 바꾸고 `PT`/`CS` 검증을 추가하면 두 경로가 같아진다.

- **[PersonFaceService.java:71](src/main/java/AirPort/service/PersonFaceService.java:71) 사진 사전검증(rejectReason)에 테스트가 없다**
  형식(JPG/PNG 선두 바이트)·용량(4MB)·base64 유효성 판정은 사용자에게 직접 노출되는 분기인데 자동 테스트가 없다. `private static` 이라 그대로는 호출할 수 없으니, 패키지 프라이빗으로 낮추거나 `uploadPicture` 를 통해 검증하는 테스트를 두면 된다(BiostarX mock).
  덧붙여 두 곳([PersonFaceService.java:74](src/main/java/AirPort/service/PersonFaceService.java:74), [BiostarUserAdapter.java:363](src/main/java/AirPort/adapter/BiostarUserAdapter.java:363))이 `Base64.getDecoder()`(strict)를 쓴다. 현재 프론트(FileReader·canvas)는 개행 없는 표준 base64를 보내므로 문제 없지만, 개행이 섞인 입력이 오면 "사진 데이터가 올바르지 않습니다"로 잘못 안내된다 — `getMimeDecoder()` 가 안전하다.

- **[CodeValidationService.java:36](src/main/java/AirPort/service/CodeValidationService.java:36) 코드 삭제·사용중지 후 기존 행 수정이 막힐 수 있다**
  검증이 저장 경로 전체에 걸리므로, 사용자 코드(`user_input='Y'`)를 삭제하거나 미사용으로 바꾸면 **그 코드를 참조하던 기존 행의 수정 저장이 실패**한다(값을 안 건드려도 폼이 그대로 되전송되므로). 현재 DB 기준 해당 데이터는 **0건**(tb_card CDT/PT/CS, tb_person UT/PS, tb_company CO 전부 확인)이라 즉시 회귀는 없다.
  → 운영에서 코드를 정리할 때 참조 행을 먼저 옮기도록 안내하거나, 검증을 "값이 바뀐 경우에만" 적용하는 방식을 검토.

- **[excel-import.js:44](src/main/resources/static/js/core/components/excel-import.js:44) 엑셀 실패 사유가 토스트 5건까지만 보인다**
  이번에 코드 오타를 잡아내게 되면서 실패 행이 늘어날 수 있는데, 100행 중 40행이 실패해도 화면에는 5건 + "외 35건"만 남고 토스트는 곧 사라진다. 어느 행을 고쳐야 하는지 다시 확인할 방법이 없다.
  → 결과를 모달/패널로 남기거나(행 번호 + 사유 목록), 실패 목록만 엑셀로 내려받게 하는 편이 낫다.

- **[CommonService.java:131](src/main/java/AirPort/service/CommonService.java:131) 시스템 코드 사용유무 강제가 조용히 일어난다**
  화면을 우회해 `use_yn='N'` 을 보내면 서버가 값을 `'Y'` 로 덮어쓰지만, 감사 문구는 평범한 "공통코드 수정"이라 시도 흔적이 남지 않는다. 정상 사용자에게는 무해하나 우회 시도 추적은 불가능하다.
  → 강제가 발생한 경우 감사 문구에 한 줄 덧붙이면 충분하다.

## 🟢 양호

- **불변식 위반 0** — 신규 SQL은 전부 mapper XML, 파라미터 `#{}` 만 사용(`${}` 추가 없음), Java 내 인라인 SQL·`@Entity`·JpaRepository 도입 없음, Controller 의 mapper 직접 의존 없음, 외부 호출은 `adapter` 계층 안에서만.
- **비밀값 유출 없음** — 변경 21커밋 전수 스캔에서 접속정보·키·토큰 검출 0. 실제 BiostarX IP/비밀번호는 git-ignored `application-local.properties` 에만 있다.
- **정합성 결함을 정확히 잡았다** — 인원 삭제 시 카드가 '발급중'으로 남던 버그([PersonService.java:222](src/main/java/AirPort/service/PersonService.java:222))를 고치면서 BiostarX 삭제 실패 시에는 회수도 하지 않도록 롤백 정책을 유지했고, 이미 잘못 남아 있던 2건은 DB에서 정리했다.
- **오류 사유가 사용자에게 도달한다** — `BiostarAdapter.responseError` 가 4xx/5xx 본문의 `Response.message/code` 를 먼저 쓰도록 바꿔 "HTTP 400"만 노출되던 문제를 해소했고, 원문은 warn 으로 남겨 사후 추적이 가능하다. 사진 데이터가 로그에 통째로 남지 않게 300자로 절단한 것도 적절.
- **조용한 실패를 제거했다** — upload_picture 가 200 이어도 템플릿 2종이 모두 비면 실패 처리해, 얼굴 없는 사용자가 저장되는 경로를 막았다.
- **감사 이력 유지** — 삭제 시 회수 장수까지 남기고(`정규인원 삭제: P001 (카드 1장 회수)`), BiostarX 실패는 `logAlways`(REQUIRES_NEW)로 롤백에도 살아남는다.
- **테스트가 변경과 함께 늘었다** — 43건 전부 통과. 이번 추가분: 코드 검증 4, 카드 회수 2, 시스템코드 2, 응답 오류 4. 모두 DB/Spring 없이 도는 순수 단위 테스트.
- **규칙 대장을 갱신했다** — `conventions.md`(배지 class·모달 폭/스크롤·코드 검증), `database.md`(삭제 시 카드 처리·검색 유형·시스템코드 고정), `security.md`(세션 만료·알려진 공백), `integration.md`(오류 사유 규칙·얼굴 실패 처리)에 신규 패턴이 모두 등록됐다. XSS 방어도 유지 — `badge.js` 가 텍스트를 이스케이프한다.

## 참고 — 이번 검토에서 확인만 하고 넘긴 것

- `PersonService.deleteOne` 이 인원은 소프트삭제하면서 출입권한(`tb_person_ac_group`)은 물리삭제한다. 화면에 복구 기능이 없어 실질 문제는 없다.
- 엑셀 업로드는 카드·기관·정규인원 3곳이 전부이며 모두 코드 검증이 걸렸다(누락 없음).
- 테스트에서 `PersonService` 생성자에 `codeValidator=null` 을 넘기는 곳이 있다([PersonServiceSearchTest.java:35](src/test/java/AirPort/PersonServiceSearchTest.java:35), [PersonDeleteCardReleaseTest.java:51](src/test/java/AirPort/PersonDeleteCardReleaseTest.java:51)). 해당 테스트가 `validate()` 경로를 타지 않아 지금은 통과하지만, 나중에 경로가 바뀌면 NPE 로 깨진다.

---

## 재검토 (2026-07-30, 🟡 5건 반영 후)

사용자 승인에 따라 권고 5건을 모두 보완했다. 테스트 **50건 전부 통과**(43 → 50), `spotlessCheck` exit 0, code-lint·docs-lint ✅.

| # | 권고 | 조치 |
|---|------|------|
| 1 | 카드 탭 경로 검증 누락 | `validate(CardForm, prev)` 를 인스턴스 메서드로 바꾸고 `PT`/`CS` 검증 추가 — 카드등록관리와 같은 규칙이 걸린다 |
| 2 | 사진 검증 테스트·strict 디코더 | `rejectReason` 을 패키지 프라이빗으로 낮춰 테스트 5건 추가, `Base64.getMimeDecoder()` 로 교체(어댑터 `normalizeTemplate` 도 동일) |
| 3 | 코드 정리 후 기존 행 수정 차단 | `validate(cmmId, codeId, label, prevCodeId)` 오버로드 — **저장된 값과 같으면 통과**. 카드·인원·기관 수정 경로가 기존 값을 넘기도록 배선 |
| 4 | 엑셀 실패 사유 5건 제한 | 결과를 모달 안 `.import-result` 패널에 **전부** 남기고, 실패가 있으면 모달을 닫지 않는다(성공분은 즉시 목록 반영) |
| 5 | 사용유무 강제가 조용함 | 강제가 실제로 일어난 경우에만 감사 문구에 `(시스템 코드 — 요청된 사용유무 'N' 을 '사용'으로 강제)` 를 덧붙인다 |

### 실증 확인 (실행 중인 앱 + DB)

- ①: 인원 저장에 없는 패스구분(`PT99`)을 담은 카드를 넣으면 → `패스구분 코드 'PT99' 가 없습니다...` 로 거부.
- ③: `CS05` 를 사용중지로 만든 뒤 그 상태인 카드를 **상태를 그대로 둔 채 수정** → `수정되었습니다`(통과). 같은 카드에서 **없는 코드로 바꾸려 하면** → 거부. 검증용으로 건드린 데이터(카드 16, 공통코드 CS05·CO99, 기관 TSTZ01)는 모두 원복 확인.

### 이번 검증 중 관찰한 별건 (수정하지 않음)

기관 수정 API 에 **필드 일부만 담은 JSON** 을 보내면 `E500 처리 중 오류가 발생했습니다` 가 나온다. 유효한 코드를 가진 기존 기관(대조군)에서도 동일하게 재현되므로 이번 변경과 무관한 기존 동작이며, 화면은 항상 전체 폼을 보내 발생하지 않는다. 다만 잘못된 요청은 4xx 가 맞다 — 별도 과제로 남긴다.
