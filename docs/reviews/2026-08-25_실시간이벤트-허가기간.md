# 리뷰: 실시간 이벤트 허가 기간

- 일시: 2026-08-25 / 검토자: sjpark2
- 대상: `main` fb3138e..82bed80 (2 커밋, 14 files, +274 −4)
- 결과: 🔴 0 · 🟡 3 · 🟢 6 → **🟡 3건 모두 조치 완료**(재검토 참고)

## 기계 검증
`gradlew test` 전체 통과 · `code-lint`(7항목) · `docs-lint` · `schema-drift` 통과.
새 SQL 2개는 **실제 MSSQL 에서 실행**해 문법·형식·결과를 확인했다(2016 미지원 함수 없음).

---

## 🔴 차단
없음.

- **비밀값**: 변경분 스캔 0건. 폐쇄망 문서의 `127.0.0.1`·`192.168.*` 는 루프백과 와일드카드로, 접속정보가 아니다.
- **계층**: 컨트롤러 변경 없음. 새 로직은 전부 service·mapper 안이다.
- **MyBatis**: 새 SQL 2개 모두 mapper XML, 파라미터 전부 `#{}`. `${}`·문자열 연결·Java 내 인라인 SQL 없음. JPA 도입 없음.
- **외부 격리**: 새 외부 호출 없음.
- **감사**: 화면 구독 시점의 기존 감사를 그대로 둔다. 표시 항목이 하나 늘었을 뿐 새 행위가 생기지 않았다.
- **관례**: 메소드 명명이 `select{대상}By{조건}`·도메인 특화 규칙에 맞고, `resultType` 은 엔티티다.

---

## 🟡 권고

### 🟡-1 "구역과 같은 방문을 본다"는 규칙이 SQL 에만 있고 테스트가 없다
[TbVisitMapper.xml:309](../../src/main/resources/mapper/TbVisitMapper.xml) · [TbVisitMapper.xml:294](../../src/main/resources/mapper/TbVisitMapper.xml)

이번 변경의 **핵심 불변식**은 허가구역과 허가기간이 *같은 방문*에서 나온다는 것이다.
두 쿼리가 각각 `MAX(visit_no)` 와 `ORDER BY vp.visit_no DESC` 로 같은 방문을 고르는데,
이 일치는 **SQL 안에만 있다.**

단위 테스트는 두 매퍼를 각각 목(mock)으로 세우므로, 한쪽 쿼리의 정렬이나 기준이 바뀌어도
**테스트는 그대로 초록이다.** 화면에는 구역과 기간이 서로 다른 방문의 값으로 섞여 나오는데,
둘 다 그럴듯한 값이라 눈으로는 알아채기 어렵다.

개발 DB 에서 두 기준의 불일치가 0건임을 실측했지만, 그것은 **오늘의 데이터**에 대한 확인이지 회귀 방지가 아니다.

**제안**: 두 쿼리를 함께 도는 통합 테스트(같은 인원에 방문 2건을 만들고 구역·기간이 같은 방문에서 나오는지)를 두거나,
방문 선택을 `<sql>` 조각 하나로 뽑아 두 쿼리가 **문자 그대로 같은 기준**을 `<include>` 하게 한다. 후자가 더 확실하다.

### 🟡-2 정규인원은 인증 1건마다 `tb_person` 을 두 번 읽는다
[MonitorService.java:237](../../src/main/java/AirPort/service/MonitorService.java) · [MonitorService.java:309](../../src/main/java/AirPort/service/MonitorService.java)

`enrich` 가 `selectById` 로 인원을 읽고, 허가기간 때문에 같은 행을 `selectAccessPeriod` 로 한 번 더 읽는다.
`selectById` 가 이미 출입기간을 갖고 있지만 **분까지**라 초를 위해 다시 읽는 구조다.

목록·수정 화면이 쓰는 `listColumns` 를 초까지로 올리면 그쪽 `datetime-local` 입력이 깨지므로
분리 자체는 옳은 선택이다. 다만 실시간 화면은 출근 시간에 인증이 몰리는 곳이라 질의 1회가 그대로 곱해진다.

현재는 보강이 단일 스레드 + 대기열 50 이라 체감 문제가 되기 어렵고, 이미 인증사진 HTTP(최대 10초)가
훨씬 무거운 구간이다. **지금 고칠 필요는 없다고 판단해 그대로 두었다.**

**나중에 필요해지면**: `listColumns` 에 초까지의 별칭 컬럼을 하나 더 얹어(`access_start_full` 등) 한 번에 읽는다.

### 🟡-3 `MonitorService` 가 469줄로 상한(500)에 가까워졌다
[MonitorService.java](../../src/main/java/AirPort/service/MonitorService.java)

이번에 46줄이 늘었다. 아직 `code-lint` 를 통과하지만 남은 여유가 31줄이다.
이 클래스는 소켓 수명·구독자 관리·이벤트 보강·감사를 함께 들고 있어 다음 기능에서 넘길 가능성이 높다.

**제안**: 다음에 이 파일을 건드릴 때 **이벤트 보강**(`enrich`/`period`/`range`/`areaNos`/`affiliationOf`)을
별도 클래스로 떼어낸다. 지금 쪼개면 이번 변경과 무관한 diff 가 섞이므로 하지 않았다.

---

## 🟢 양호

- **삭제된 방문이 잡히지 않는 이유를 확인하고 넘어갔다.** 새 쿼리에 `v.del_yn = 'N'` 이 없어 소프트 삭제된
  방문의 기간이 보일 수 있다고 의심했으나, 방문 삭제 경로가 `clearRoster → deletePersons` 로
  **`tb_visit_person` 행을 물리 삭제**한다. 그 표를 거쳐 가는 두 쿼리는 삭제된 방문에 닿을 수 없다.
  개발 DB 에서도 삭제된 방문에 걸린 방문객 0건을 확인했다. 기존 구역 쿼리와 같은 이유로 안전하다.
- **입력 화면을 깨뜨리지 않는 길을 골랐다.** 공유 `listColumns` 의 정밀도를 올리는 대신 전용 조회를 뒀다.
  올렸다면 인원 등록·수정의 `datetime-local` 입력이 값을 못 받아 **조용히 빈칸**이 됐을 것이다.
- **없는 값을 지어내지 않았다.** 초를 `:00` 으로 붙이지 않고 DB 에서 `varchar(19)` 로 다시 읽는다.
- **화면을 수치로 검증했다.** 값이 41자로 늘어 1920/1600/1366 세 폭에서 넘침을 실측하고(1920 기준 454/407px),
  고친 뒤 넘침 0 을 다시 실측했다. 그 과정에서 **붙임표(`-`)가 줄바꿈 자리로 쓰여** 날짜 하나가 두 줄에
  갈라지는 것을 발견해 `<span>` 묶음으로 막았다 — 화면을 열어 보지 않았다면 못 잡았을 결함이다.
- **규칙 대장에 등록했다.** 위 두 함정(정밀도 분리, 좁은 칸의 날짜 줄바꿈)을 `docs/conventions.md` §6·§2 에 남겼다.
- **XSS 경로를 확인했다.** `periodHtml` 이 `innerHTML` 을 쓰지만 두 조각 모두 기존 `esc()` 를 거치고,
  값이 서버가 만든 날짜 문자열이라 사용자 입력이 섞이지 않는다.

---

## 참고 — 검토 중 발견 (이번 변경 범위 밖)

### 방문 데이터 정기 파기가 현장에서 한 건도 지우지 않는다
[VisitPurgeService.java:45](../../src/main/java/AirPort/service/VisitPurgeService.java) · [application.properties:94](../../src/main/resources/application.properties)

`app.visit.purge.dry-run` 이 **jar 기본값·현장 배포 예시 양쪽 다 `true`** 라, 배치는 매일 돌지만
대상만 세고 지우지 않는다. 감사추적에 남은 실행 이력이 전부 `미리보기 — 실제로 지우지 않음` 이다.

개발 DB 실측: 2026-08-25 14:10 실행이 **대상 1건(방문번호 1, 퇴실 2025-07-01)을 찾아 놓고 지우지 않았고**,
그 방문은 지금도 남아 있다.

미리보기가 기본인 것은 의도된 설계다(첫 실행에 대량 삭제 사고를 막는다). 다만 **개인정보 보존기간 파기는
지켜야 할 의무**라, 현장에서 확인이 끝났다면 `false` 로 켜야 한다. 켜는 것을 잊기 쉬운 구조라
기동 로그에 한 줄을 남기고 있다.

아울러 대상 조건이 **퇴실 완료(VS04) + `checkout_dt` 기준**이라, 퇴실 처리가 되지 않은 채 방치된 방문은
작업기간이 아무리 지나도 영원히 대상이 되지 않는다. 현장 데이터에서 그런 방문이 쌓이는지 확인이 필요하다.

## 조치
- 🟡-1·2·3: **모두 수정 완료** (아래 재검토 참고).
- 참고 항목: 설정·운영 사안이라 코드 변경 없음. 현장 `config/application.properties` 에서 결정한다.

---

# 재검토 (2026-08-25, 권고 조치 후)

`gradlew test` 전체 통과 · `code-lint`(7항목) · `docs-lint` · `schema-drift` 통과.
새 SQL 은 실제 MSSQL 에서 실행해 확인했고, 앱을 띄워 화면 5개(200)와 실시간 이벤트 구성 요소를 실측했다(콘솔 오류 0건).

## 🟡-1 — 방문 선택 기준을 조각 하나로 모으고, 깨지면 실패하는 검사를 붙였다

기준을 `<sql id="latestVisitOfPerson">` 하나에 두고 허가구역·허가기간 두 쿼리가 함께 `<include>` 한다.
기간 쿼리는 `tb_visit_person` 을 다시 훑을 이유가 없어져 `TOP 1`·`ORDER BY` 가 사라지고 `tb_visit` 단건 조회가 됐다.

[MonitorPeriodRuleTest](../../src/test/java/AirPort/service/MonitorPeriodRuleTest.java) 가 mapper XML 을 직접 읽어
**두 쿼리가 모두 그 조각을 쓰는지** 본다. 규칙이 SQL 안에만 있어 목(mock) 기반 단위 테스트로는 깨져도 초록이기 때문이다.

> **검사가 실제로 잡는지 확인했다.** 기간 쿼리가 제 손으로 방문을 고르도록 되돌려 놓고 돌려 실패를 확인한 뒤 원복했다.
> 실패하지 않는 검사를 두는 것은 없느니만 못하다.

## 🟡-2 — 인증 1건당 인원 질의 3회 → 1회

화면 전용 단건 조회 `TbPersonMapper.selectForMonitor` 를 두고 성명·소속·기관명·출입기간(초까지)을 한 번에 읽는다.
`selectAccessPeriod` 는 사라졌고, 기관명을 따로 묻던 `companyMapper.selectById` 호출도 없어졌다(조인이 함께 준다).

| | 이전 | 이후 |
|---|---|---|
| 정규인원 1건 | `selectById` + `selectAccessPeriod` + `company.selectById` = **3회** | `selectForMonitor` = **1회** |
| 방문객 1건 | `selectById` + `company.selectById` = 2회 (+ 방문 조회) | `selectForMonitor` = 1회 (+ 방문 조회) |

`listColumns` 의 정밀도를 올리지 않은 이유는 그대로다 — 등록·수정 화면의 `datetime-local` 이 조용히 빈칸이 된다.

## 🟡-3 — 이벤트 보강을 떼어냈다

[MonitorEnrichService](../../src/main/java/AirPort/service/MonitorEnrichService.java) 를 새로 만들어
`enrich`·`period`·`areaNos`·`time` 등 **한 건을 화면 모양으로 만드는 일**을 옮겼다.
`MonitorService` 는 소켓 수명·구독자·감사만 든다.

```
MonitorService      469줄 → 294줄 (상한 500)
MonitorEnrichService        209줄 (신규)
```

의존도 함께 줄었다 — `MonitorService` 가 들고 있던 매퍼 5개 중 4개가 보강 쪽으로 갔다.

## 🟢 이번 조치에서

- **죽은 코드를 남기지 않았다** — `selectAccessPeriod`, `TbCompanyMapper` 주입, `decrypt`/`areaNos`/`time` 의 옛 자리 모두 정리했다(잔재 0건 확인).
- **리팩터링이 배선을 깨지 않았음을 띄워서 확인했다** — 클래스를 나누면 Spring 주입이 어긋나도 컴파일은 통과한다. 실제로 기동해 화면 5개와 실시간 이벤트 요소를 확인했다.
- **규칙 대장에 한 줄 더 등록했다** — "건마다 도는 화면은 전용 단건 조회를 둔다"(`conventions.md` §6).
