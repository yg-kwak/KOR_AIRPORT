# 리뷰: /cleanup — NUL 바이트 수정 + 죽은 mapper 제거

- 일시: 2026-07-15 / 검토자: SUPREMA\sjpark2
- 대상: `/cleanup` 정리 diff (BiostarSession NUL 수정, TbSystemMapper.countAll 제거)
- 결과: 🔴 0 · 🟡 0 · 🟢 3

## 🔴 차단
없음.

## 🟡 권고
없음.

## 🟢 양호
- **[BiostarSession.java:91](../../src/main/java/AirPort/adapter/BiostarSession.java)** — 문자열 리터럴의 원시 NUL 바이트(0x00)를 유니코드 이스케이프(백슬래시 u0000)로 치환. 런타임 동일(세션키 구분자 NUL 유지), 소스는 순수 ASCII로 정상화. §4 불변식 무관, 순수 소스 위생.
- **`TbSystemMapper.countAll` 제거**([java](../../src/main/java/AirPort/mapper/TbSystemMapper.java)+XML) — 호출처 0, 단일행 판정은 `selectOne()==null` 로 대체됨. 미사용 쿼리 제거(계층·MyBatis 규약 영향 없음).
- **`updateLoginFailCnt` 유지 결정** — 미사용이나 문서화된 '계정 잠금' 계획 기능(security.md:16) 스캐폴딩이라 보존(사용자 승인).

## 검증
- code-lint(파일크기·[2]~[5]) 통과, `./gradlew test`(ArchUnit 포함) 통과, smoke **84/0**.
- 전 `src/**/*.java` NUL 0·정상 UTF-8 재확인.

## 조치
- 삭제/수정 모두 반영. 별도 후속 없음.
