# 리뷰: 인원·카드·출입그룹 스키마 설계 (tb_person / tb_person_photo / tb_card / tb_person_ac_group)
- 일시: 2026-07-21 / 검토자: SUPREMA\sjpark2
- 대상: main 3f52fa8 + 워킹트리(미커밋) — `sql/ddl/01_tables.sql`, `docs/database.md`
- 결과: 🔴 0 · 🟡 6 · 🟢 9

변경 성격: **DDL + 문서만**. Java/Mapper/화면 코드 없음 → 계층경계·MyBatis·adapter 항목은 이번 대상 아님(신규 4테이블).

## 사전 확인 (중요)
워킹트리 시작 시점에 `tb_car`·`tb_company` 가 DDL에서 빠져 있어 재작성했으나, **HEAD 버전과 바이트 단위로 일치**함을 확인(중복 정의·기존 Car/Company 코드 훼손 없음). 순수 증분은 신규 4테이블뿐이다.

## 🔴 차단
없음.

## 🟡 권고
- [docs/security.md:30] **Enc=Y 컬럼 목록이 stale**. `tb_login_user.user_name/password` 만 나열되어 있으나 실제로는 `tb_system.biostar_pw`·`tb_company.ceo_name`·신규 `tb_person.person_name/birth_date/person_phone` 까지 늘었다 → 괄호 안 예시를 갱신하거나, "단일 출처는 database.md 의 Enc=Y" 문장만 남기고 예시를 제거해 드리프트 원천을 없앨 것.
- [sql/ddl/01_tables.sql] **`tb_person_photo.photo_data` 암호화 여부 미결**. 얼굴 사진은 생체정보로 일반 개인정보보다 보호 수준이 높은데 평문 BASE64로 저장된다. 암호화/비암호화를 **명시적으로 결정**하고 `database.md` Enc 열에 반영할 것(현재는 결정 자체가 문서에 없음).
- [sql/ddl/01_tables.sql] **`tb_card.biostar_card_value` 평문 저장**. 사용자가 "암호화 안 함"으로 결정했으나 실물 카드값은 사실상 출입 크리덴셜이다. security.md:43 의 "추가 암호화 대상 확정 TODO" 와 함께 **결정 근거를 문서에 남길 것**.
- [sql/seed/02_seed.sql] **공통코드 시드 5종 누락**: 신규 테이블이 참조하는 `UT`(직위)·`PT`(발급유형)·`CDT`(카드구분)·`CS`(카드상태)·`IS`(발급구분) 이 시드에 없다(`CO` 5건·`LO`·`AT` 만 존재). 신규 환경에서 DDL 실행 시 해당 select 가 전부 비게 된다 → 시드 추가 필요. (운영 DB에 직접 넣었더라도 저장소 시드가 신규 환경의 원천)
- [tb_person_ac_group] **권한 부여/회수 이력이 DB에 남지 않는다**(현재 상태만 보관, 매핑 행 삭제 시 소멸). 서비스 구현 시 인원명·출입그룹명을 **스냅샷으로 `tb_system_log` 에 반드시 기록**할 것. 누락하면 "특정 시점에 누가 어느 구역 권한을 가졌나"를 복원할 수 없다(공항 보안감사 관점 리스크). DB에서 직접 재구성이 필요하면 별도 이력 테이블 검토.
- [tb_card] **`person_id` 인덱스 없음**. "이 인원의 카드 목록" 이 주 조회 패턴이므로 비클러스터 인덱스 추가 권고.

## 🟢 양호
- 비밀값·접속정보 하드코딩 없음.
- 암호화 컬럼을 `nvarchar(255)` 로 잡아 hex 확장 여유분 확보 — security.md:38 규칙 준수.
- 성명·생년월일·연락처를 Enc 대상으로 지정해 개인정보 암호화 불변식 충족.
- **암호문 검색·정렬 불가 제약을 스키마 문서에 경고 블록으로 명시** — 구현 단계에서 뒤늦게 발견되는 함정을 사전 차단(TbLoginUserMapper.xml:34 의 기존 제약과 동일 선상).
- `use_yn`(활성/비활성) / `del_yn`(소프트 삭제) 구분을 일관 적용 + CHECK 제약. 소프트 삭제로 조인·이력 보존.
- 명명 규칙 준수: `tb_` 접두·snake_case·`PK_`/`CHK_` 제약 네이밍·공통 감사 컬럼(reg_dt/mod_dt).
- **`tb_person` 을 `tb_login_user` 와 이름 분리** — 동일 `user_id` 로 두 개체가 공존해 조인을 오용할 위험 제거.
- **`card_status` 단일 컬럼화**(정지 전용 플래그 제거) — 상태가 두 곳에 표현돼 모순 저장되는 구조 차단. 사유를 DDL 주석에 남겨 재발 방지.
- **사진 테이블 분리**(행 크기·목록 성능·생체정보 보호) + **복합 PK 로 중복 권한 부여 차단**.
- 검증: `scripts/schema-drift.sh` ✅(테이블 14종·전 컬럼·역방향) / `scripts/docs-lint.sh` ✅.

## 조치
- 🟡-1 (security.md Enc 목록): **반영** — `security.md` 의 Enc=Y 목록을 7개 컬럼으로 갱신하고, 판정 기준이 `database.md` 의 `Enc` 열임을 명시.
- 🟡-2 (사진 암호화 결정): **반영(암호화 안 함으로 확정)** — `security.md`「비암호화 결정(확정)」에 근거 기록. 대신 별도 테이블 분리로 노출면 축소. `database.md` 표에도 표기.
- 🟡-3 (카드값 근거 기록): **반영(암호화 안 함으로 확정)** — 근거 "카드값은 개인정보가 아님" 을 `security.md`·`database.md` 양쪽에 기록.
- 🟡-4 (공통코드 시드 5종): **미반영** — `UT`·`PT`·`CDT`·`CS`·`IS` 시드 추가 필요(별도 작업).
- 🟡-5 (권한 이력 감사): **반영** — `security.md`「감사 이력」에 *출입권한 부여/회수는 반드시 남긴다* 규칙을 필수로 명문화(부여=CREATE/회수=DELETE, 인원명·출입그룹명 스냅샷). 구현 시 준수 대상.
- 🟡-6 (person_id 인덱스): 보류 — 성능 튜닝 시점에 판단.
