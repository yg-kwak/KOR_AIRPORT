# 리뷰: 방문구분(인원/차량/인원+차량) · 키오스크 [등록 수정] · 방문 모달 개편
- 일시: 2026-09-15 / 검토자: sjpark2
- 대상: main eca34fc..80793fd (8 커밋 — 3b5ad79, fd54c26, cb39adf, 6478df5, 095d6d1, a36c13f, a78fc65, 80793fd)
- 결과: 🔴 0 · 🟡 6 · 🟢 7

## 🔴 차단
없음

## 🟡 권고
- 🟡-1 **[우선] 키오스크 수정이 카드가 붙은 방문객·차량을 지우면서 카드를 회수하지 않는다** — [KioskVisitService.java:172](../../src/main/java/AirPort/service/KioskVisitService.java:172), [:181](../../src/main/java/AirPort/service/KioskVisitService.java:181).
  신청(VS01) 상태만 열지만, 관리자가 **일부** 방문객·차량에 카드를 붙여도 전원이 아니면 상태는 VS01 그대로다. 그 방문을 키오스크에서 고치며 그 사람을 빼면 `personMapper.softDelete` 만 하고 `cardMapper.releaseByPerson` 이 없어 **카드가 지워진 사람에게 묶인 채 남는다**(차량도 `releaseByCar` 없음). 관리자 화면(`VisitRosterService.saveChildren`)은 회수 후 소프트삭제한다.
  → 빠지는 방문객·차량의 카드를 회수하거나(관리자 화면과 같게), 카드가 붙은 방문객이 하나라도 있으면 키오스크 수정을 거절(안내: "카드가 발급된 신청은 관리자에게 문의")하는 편이 단순하다. 같은 이유로 BiostarX 동기화가 **부분 실패**해 VS01 로 되돌아온 방문(`biostarUserId` 가 있는 방문객)도 키오스크가 지우면 장비에만 사용자가 남는다 — 거절 조건에 `biostarUserId != null` 을 함께 둔다.
- 🟡-2 **인솔자 성명이 URL 쿼리로 다닌다** — [KioskController.java:78](../../src/main/java/AirPort/controller/KioskController.java:78), [:86](../../src/main/java/AirPort/controller/KioskController.java:86), [:96](../../src/main/java/AirPort/controller/KioskController.java:96).
  `GET /kiosk/visit/mine?managerName=홍길동` 은 접근 로그·프록시·키오스크 브라우저 이력에 **성명(개인정보) 평문**이 남는다. 키오스크는 공용 단말이다.
  → 조회·상세도 `POST` 본문으로 받는다(`{managerId, managerName, visitNo}`). 서버 판정(`requireMine`)은 그대로.
- 🟡-3 **무인증 목록 응답에 인솔자 성명 암호문·소속이 실려 나간다** — [KioskVisitService.java:115](../../src/main/java/AirPort/service/KioskVisitService.java:115).
  `selectAppliedByManager` 가 목록용 `cols`(`manager_name`·`manager_affiliation`·`manager_company_name`) 를 그대로 쓰고 키오스크는 복호화하지 않아 **ARIA 암호문**이 JSON 으로 나간다. 화면은 쓰지 않는 값이다. 암호문이라 읽히지는 않지만 무인증 응답에 넣을 이유가 없다.
  → `applied()` 가 방문번호·작업기간·작업목적·방문객 수만 담은 응답으로 줄이거나(`KioskVisitRow` 같은 `*Result`), 매퍼가 그 컬럼만 고른다.
- 🟡-4 **키오스크 상세 조회에 감사 기록이 없다** — [KioskVisitService.java:36](../../src/main/java/AirPort/service/KioskVisitService.java:36) (`detail()`).
  §4 "데이터 조회는 tb_system_log" — `detail()` 은 방문객 성명·생년월일·인솔자 연락처를 **복호화해 돌려준다**. 등록·수정은 남기는데(`:103`, `:187`) 조회는 남기지 않는다. 무인증이라 "누가 어느 방문을 열었나" 가 더 중요하다.
  → `auditService.log(null, READ, null, "키오스크 방문 조회: " + visitNo + " (인솔자 " + managerId + ")")`. 목록(`applied`)은 실패(인원ID·성명 불일치)도 남기면 무작위 대입 시도가 드러난다.
- 🟡-5 **완전수식명** — [KioskVisitService.java](../../src/main/java/AirPort/service/KioskVisitService.java) · [VisitService.java](../../src/main/java/AirPort/service/VisitService.java): `AirPort.common.VisitKinds.check(...)`, `AirPort.model.VisitManagerForm.encrypted(...)` 가 10곳. 2026-08-18 리뷰(🟡-4)에서 한 번 정리한 항목이다. → import 로.
- 🟡-6 **탭이 없어진 뒤 남은 선택자** — [style.css:124](../../src/main/resources/static/css/style.css:124) `.visit-modal.readonly .modal-body .btn:not(.tab-btn)` — 방문 모달에 `.tab-btn` 이 더 없다. → `:not(.tab-btn)` 제거.

## 🟢 양호
- **비밀값 없음**: 커밋 전 스캔 통과, 실 IP·접속정보 없음. 배포 스크립트(`sql/deploy/2026-09-15_update.sql`)는 재실행 안전 + 백필.
- **계층 경계**: 새 엔드포인트 셋(`/mine`·`/detail`·`PUT`)이 모두 `KioskVisitService` 로만 나가고 컨트롤러에 판정이 없다. 판정은 `requireMine` **한 곳**을 목록·상세·수정이 함께 지난다([KioskVisitService.java](../../src/main/java/AirPort/service/KioskVisitService.java)) — 목록만 거르면 방문번호로 뚫리는 구멍을 막았다.
- **MyBatis 규약**: 새 SQL(`selectAppliedByManager`, `selectVisitorLastCardName`) 모두 XML, `#{}` 만. `visit_kind` 는 cols/INSERT/UPDATE 세 곳에 있고 `VisitKindsTest` 가 그것을 지킨다.
- **암호화**: 인솔자 성명 판정을 **암호문끼리** 비교(`encryptOrNull`)한다. 평문 비교였다면 조용히 아무것도 못 찾는다.
- **무인증 입력 방어**: 클라이언트가 보낸 방문객 `personId` 를 그 방문의 명단에 있는 것만 믿는다 — 정규인원까지 덮어쓰는 길을 막았고 목 테스트로 고정했다. 상태·유형은 서버가 임시·신청으로 고정.
- **규칙 한 곳**: 방문구분 규칙(`VisitKinds.check`)을 임시·장기·키오스크 세 화면이 공유하고, 어긋나면 **거절**한다(조용히 버리면 카드가 사라진다). 공통코드가 아니라 코드가 원천인 이유를 문서(`conventions.md`, `database.md`)에 남겼다.
- **테스트**: `KioskEditRuleTest`(5) · `VisitKindsTest`(5) · `VisitServiceStrictTest` +3(차량만 입실 승격·거부) — SQL 안에만 있는 조건은 XML 을 읽는 규칙 테스트로 잡았다.

## 조치
- 🟡-1: 반영 — `KioskVisitService.requireNoCards`: 카드가 붙었거나 `biostarUserId` 가 있는 방문객·카드가 붙은 차량이 하나라도 있으면 키오스크 수정을 거절("카드가 발급된 신청은 키오스크에서 고칠 수 없습니다. 관리자에게 문의하세요."). 회수·장비 삭제는 관리자 화면의 일이라 거절이 가장 단순하다. `KioskEditRuleTest` 에 세 경우(방문객 카드·BiostarX·차량 카드) 추가.
- 🟡-2: 반영 — 목록·상세·수정 모두 본문(`KioskEditForm {managerId, managerName, visitNo, form}`)으로. `POST /mine`·`POST /detail`·`PUT`(GET 은 없앴다). 화면 실측으로 요청 URL 에 성명이 없는 것을 확인.
- 🟡-3: 반영 — `applied()` 가 `KioskVisitResult`(방문번호·구분·기간·목적·방문객·차량 수)만 돌려준다. 목 테스트로 인솔자 성명 값이 응답에 없는 것을 고정.
- 🟡-4: 반영 — 상세 조회 `READ` 감사, 인원ID·성명 불일치도 `READ` 로 남긴다(성명은 적지 않음). 개발 DB 에서 조회·수정·불일치 세 줄이 남는 것을 확인.
- 🟡-5: 반영 — `VisitKinds`·`VisitManagerForm`·`TbPerson` import 로 정리(서비스 둘 + 테스트 둘).
- 🟡-6: 반영 — `:not(.tab-btn)` 제거.

## 재검토
- 일시: 2026-09-15 / 대상: 반영 커밋(아래 커밋 해시) / 결과: 🔴 0 · 🟡 0
- 반영 여섯 건을 다시 훑었다. 새 경로 셋이 여전히 `requireMine` 한 곳을 지나고(`KioskEditRuleTest` 7건), 본문 파라미터는 `KioskEditForm` 으로만 들어온다. 목록 응답에 `Tb*` 행이 나가지 않는다. 남은 지적 없음.
