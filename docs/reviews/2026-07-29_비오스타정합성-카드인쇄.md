# 리뷰: BiostarX 정합성 강화 · 카드 브라우저 인쇄 전환 · 채번/상태머신 보강

- 일시: 2026-07-29 / 검토자: SUPREMA\sjpark2
- 대상: main `ec8d89f..HEAD` (커밋 12개 — 기관코드 채번·인원 BiostarX 필수화(등록/수정), 퇴실완료 읽기전용, 마지막 카드 기록, 카드 인쇄 브라우저 전환(+중간 시도들), 정합성 강화(퇴실·삭제·블랙리스트 롤백))
- 결과: 🔴 1 · 🟡 4 · 🟢 7

## 🔴 차단 (불변식/강제 규칙 충돌)

- **[전역] spotless 포맷과 code-lint 500줄 제한이 상충 — 커밋된 코드가 spotlessCheck 실패 상태**.
  `gradlew spotlessCheck` 가 **38개 파일** 위반으로 실패한다(커밋된 내용 기준 — CRLF 아님). 원인: 여러 세션에 걸쳐 (a) 무관 파일 오염을 피하려 spotlessApply 전체 실행을 생략한 선택 스테이징, (b) 500줄 제한을 맞추려 적용한 한줄 압축(100컬럼 초과 javadoc 등)이 formatter 규칙과 충돌. **spotlessApply 를 적용하면 [PersonService.java](src/main/java/AirPort/service/PersonService.java) 525줄 / [VisitService.java](src/main/java/AirPort/service/VisitService.java) 509줄로 code-lint(≤500) 가 실패**한다 — 즉 현재 두 강제 규칙을 동시에 만족할 수 없다. CI(`gradlew clean build`)는 spotlessCheck 를 포함하므로 **CI 실패 위험**.
  → 수정 방향(택1, 사용자 승인 필요):
  (A) **서비스 분리 리팩터(권장)** — PersonService 에서 BiostarX 동기화부(syncPersonToBiostar/biostarRequest 군)를 `PersonBiostarService` 로, VisitService 에서 로스터 저장부(saveChildren/upsertVisitor/insertVisitCar/clearRoster)를 별도 서비스로 분리 → 포맷 적용 후에도 각 500줄 이하. 압축 없이 정상 포맷 유지.
  (B) **ALLOWLIST 등록** — scripts/code-lint.sh ALLOWLIST 에 두 파일을 사유와 함께 등록하고 spotlessApply 전체 커밋(빠르지만 제한 규칙의 예외가 늘어남).
  ※ 검증 중 실행한 spotlessApply 는 리뷰 규칙(수정 금지)에 따라 되돌렸다(전부 formatter 산출물임을 확인).

## 🟡 권고

- **[BiostarUserAdapter.java(userExists)] 통신 오류를 '사용자 없음'으로 간주 → 엄격 정책 우회 구멍**. `userExists` 가 예외 시 `false` 를 반환하고, [VisitBiostarService.java](src/main/java/AirPort/service/VisitBiostarService.java) 의 `disableVisitors`/`deleteVisitors` 는 `!exists` 면 **건너뛴다(성공 취급)**. BiostarX 전면 장애 시 전원이 skip 되어 **퇴실·방문삭제가 경고 없이 커밋** — 이번에 도입한 실패=롤백 정책이 정확히 그 시나리오(장비 다운)에서 무력화된다. → userExists 를 3상(존재/없음/통신오류)으로 나누거나 통신오류 시 예외를 전파해 strict 경로에서 롤백되게 권장.
- **[CardService.java:88](src/main/java/AirPort/service/CardService.java:88)** 블랙리스트 실패 감사(logAlways)의 actor 가 `null`(SYSTEM 기록) — `syncBlacklist` 가 actor 를 못 받아 실제 수행자가 감사에 안 남는다. → actor 파라미터 전달 권장.
- **[card-print.js](src/main/resources/static/js/web/person/card-print.js) 인쇄 감사 시맨틱** — `window.print()` 후 감사 API 를 부르므로 사용자가 **인쇄 대화상자에서 취소해도 "출력"으로 감사**된다(실제로는 "출력 요청" 기록). 또한 `/card/print/bulk` 는 대상 전원의 앞/뒤 PNG data URL 을 한 응답에 담아 **수십 명이면 수십 MB** — 대량 출력 시 응답 크기·메모리 주의. → 감사 문구를 "출력 요청"으로 조정하거나 afterprint 이후 확정 호출, 일괄은 페이지 단위 분할 고려.
- **[VisitService.java(effectiveStatus)]** 등록 시 전원 카드면 신청(VS01) 없이 **즉시 입실중(VS03)** — 승인 단계가 없는 흐름이 의도인지 확인 필요(직전 감사에서 지적, 이번 범위에선 유지됨). VS02(신청취소) 설정 경로 부재도 동일.

## 🟢 양호

- **실패=롤백 정책 확장이 일관되게 구현됨** — 퇴실([VisitService.checkout](src/main/java/AirPort/service/VisitService.java))·인원삭제(PersonService.deleteOne)·방문삭제 모두 BiostarX 성공 후에만 DB 변경, 실패 시 사유 예외 + 재시도 유도. 등록·수정과 동일 패턴.
- **롤백에도 남는 실패 감사** — [AuditService.logAlways](src/main/java/AirPort/service/AuditService.java)(REQUIRES_NEW) 로 실패 사실이 tb_system_log 에 보존(불변식 §4 감사 강화).
- **블랙리스트 실패=롤백** — 분실 카드가 장비에서 유효한 채 방치되는 구멍 제거. 장비 미등록 카드(biostar_card_id 없음)는 정확히 스킵.
- **채번 레이스 보강** — 서버 자동 채번(방문객 IS)은 1회 재채번-재시도, 사용자 편집 가능 PK(인원/기관/카드번호)는 유니크 충돌을 친화 메시지로 변환(임의 재채번으로 사용자 값을 바꾸지 않는 판단 적절).
- **입실중 카드 교환만 허용** — 서버 검증으로 카드 회수·방문객 제외를 차단, 상태-실물 불일치 방지.
- **테스트 동반** — [VisitServiceStrictTest.java](src/test/java/AirPort/VisitServiceStrictTest.java) 가 퇴실 롤백(카드 회수 미실행 검증 포함)·VS03 교환만 규칙을 mock 으로 커버. 전체 20 테스트 그린.
- **카드 인쇄 브라우저 전환으로 어댑터 축소** — CardPrintAdapter 에서 java.awt.print 의존 제거(템플릿 로더만 잔존), 서버 인쇄 설정 정리. 외부 연동 격리 관점에서 단순화. last_card_no 평문 저장은 문서 근거 있음(카드값은 비개인정보 — docs/database.md).

## 조치
- 🔴-1: **(A) 서비스 분리로 반영** — 아래 재검토 참조.
- 🟡-1(userExists 3상): **반영**. 🟡-2(감사 actor) · 🟡-3(인쇄 감사/대량 응답): 보류.
- 🟡-4(즉시 VS03): 업무 정책 확인 필요(보류).

## 재검토 (2026-07-29, 사용자 승인 후 수정 반영)
- **🔴-1 해소**: 책임 단위 분리로 두 강제 규칙을 동시에 만족.
  - `PersonBiostarService` 신설 — PersonService 의 BiostarX 동기화부(sync/delete/요청 구성/코드 변환) 이동. PersonService 337줄.
  - `VisitRosterService` 신설 — VisitService 의 로스터 저장부(saveChildren/upsertVisitor/insertVisitCar/clearRoster) 이동. VisitService 395줄. KioskVisitService 재배선.
  - 전체 `spotlessApply` 적용 후 **spotlessCheck GREEN + code-lint GREEN + 테스트 20건 GREEN** 확인(누적 포맷 드리프트 38파일 해소).
- **🟡-1 해소**: `BiostarUserAdapter.userExists` 가 통신 오류를 '없음'으로 처리하지 않고 `BiostarSessionException` 으로 전파. `VisitBiostarService` 3개 루프(sync/delete/disable)는 해당 오류를 실패로 집계 → 퇴실·삭제 strict 경로가 장비 전면 장애에서도 정확히 롤백. `PersonBiostarService.syncPersonToBiostar` 는 오류를 실패 사유로 변환.
- 결과: 🔴 0 · 🟡 3(보류) · 🟢 유지.
