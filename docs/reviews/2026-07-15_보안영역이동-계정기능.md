# 리뷰: 계정 자가서비스 + BiostarX 세션/시드 + 보안영역 분리 + 모델 규칙

- 일시: 2026-07-15 / 검토자: SUPREMA\sjpark2
- 대상: `main` `59b7582..5e3b246` (이번 세션 10커밋 — acGroup 매핑 보정, BiostarX 세션관리자·로컬시드, 계정 헤더메뉴, 메뉴 재편, /system→/security 이동, JS 통일, model 린트)
- 결과: 🔴 0 · 🟡 3 · 🟢 8

## 🔴 차단 (불변식 위반)
없음. AGENTS.md §4 불변식 위반 없음.

## 🟡 권고
- **[BiostarLocalSeeder.java:53-57](../../src/main/java/AirPort/config/BiostarLocalSeeder.java)** — config 계층 시더가 `TbSystemMapper` 를 직접 호출하고 `ARIAUtil.ariaEncrypt` 로 암호화한다. ArchUnit 상 위반은 아니지만(Controller 만 mapper 직접호출 금지), conventions §5 "암복호화 호출은 Service 계층에서만" 정신과는 어긋난다. → 암호화+upsert 를 `SystemService` 의 메소드로 위임하면 계층 일관성↑. (강제 아님, 개선 권고)
- **테스트 부재 (`docs/testing.md`)** — 신규 분기 로직에 단위 테스트가 없다:
  - `AccountService.changePassword`([AccountService.java:57](../../src/main/java/AirPort/service/AccountService.java)) — 이전비번 불일치/확인 불일치/이전과 동일 3분기
  - `MenuService.startMenuTarget`([MenuService.java](../../src/main/java/AirPort/service/MenuService.java)) — 권한 회수 시 "/" 폴백
  - `BiostarSession` 만료 판정(401+code10)·1회 재시도
  smoke 가 400 경로는 HTTP 로 커버하나, 서비스 단위 분기 테스트 권장(기존 MenuServiceTest 선례).
- **[AccountController.java:menus](../../src/main/java/AirPort/controller/AccountController.java)** — `GET /account/menus`(본인 접근가능 메뉴 조회)에 READ 감사가 없다. 자가서비스 UI 헬퍼라 경계선상이나, "데이터 조회 감사"(불변식6) 관점에서 기록 여부 판단 필요. (현재 미기록 — 의도라면 그대로 두어도 무방)

## 🟢 양호
- **비밀값(불변식1)**: 실 자격증명이 커밋 코드에 없음. 실값은 git-ignore 된 `application-local.properties`(`app.biostar.*`)에만, 시더가 `@Value` 주입. smoke 는 더미. 세션 diff 시크릿 스캔 clean.
- **외부 격리(불변식4)**: BiostarX 인증/세션/호출이 `adapter`(`BiostarSession`/`BiostarAdapter`)로만. 세션ID·비밀번호 로그 미기록.
- **암호화(불변식5)**: 비밀번호 변경이 ARIA 암호문 비교·저장, 복호화 안 함. 키는 프로퍼티(`app.crypto.aria-key`) 주입.
- **감사(불변식6)**: 시작메뉴·비밀번호 변경 모두 `auditService.log(UPDATE, ...)` 기록. 자가서비스라 menuId=null(LOGIN/LOGOUT 선례).
- **MyBatis(불변식3)**: 신규 `updateStartMenu`/`updatePassword` 는 XML + `#{}`. 문자열연결/`${}`/JPA 없음.
- **계층(불변식2)**: Account/Login 컨트롤러 thin(actor 추출+위임). controller→service→mapper 단방향 — ArchUnit 통과.
- **관례(불변식7)**: `/account` 자가서비스 예외 문서화(conventions §4), model 레코드/DTO 규칙 명문화 + **code-lint [5] 신설 강제**, /system→/security 이동 시 code-lint [4] 를 `/{system,security}/*` 로 확장. `Tb*`/`*SearchParam`/`*Form` 네이밍 준수.
- **menu_id 통제**: 하드코딩 없음(interceptor URL 해석). 보안영역 이동 시 seed·컨트롤러 매핑·권한상세·감사로그 menu_id 정합.
- **검증 이력**: 각 변경마다 컴파일/전체 test/smoke 84·code-lint 통과 확인. 옛 `/system/*` 500 은 앱의 기존 미매핑 처리(별도 작업으로 분리).

## 조치
- 🟡 3건은 모두 개선 권고(강제 아님). 사용자 승인 시 별도 진행:
  - 시더 암호화+upsert 를 `SystemService` 로 이동
  - `AccountServiceTest`(비밀번호 분기)·`startMenuTarget` 테스트 추가
  - `/account/menus` READ 감사 정책 결정
