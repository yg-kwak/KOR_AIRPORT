# 리뷰: 키오스크(무인증) 방문 신청

- 일시: 2026-07-27 / 검토자: SUPREMA\sjpark2
- 대상: origin/main..HEAD 의 키오스크 관련분 (817a613 · 3a7ad0d · b487305 · ed77d76) + 미커밋(kiosk 버튼/힌트 CSS·HTML)
- 결과: 🔴 0 · 🟡 4 · 🟢 6

## 🔴 차단 (불변식 위반)
없음.

## 🟡 권고
- [src/main/java/AirPort/service/VisitService.java:117](src/main/java/AirPort/service/VisitService.java) `searchManagersPublic(keyword)` — 무인증 공개 엔드포인트 `/kiosk/visit/managers` 가 **정규인원 성명(복호화)+인원ID를 반환**한다. 특히 keyword 가 빈 값이면 상위 50명 목록을 그대로 내려준다. 화면(visit.js)은 검색어를 강제하지만 **서버는 빈 keyword 를 막지 않아** 공개 URL 직접 호출로 직원 명단이 노출될 수 있다. → 서버에서 최소 검색어 길이(예 2자 이상)를 요구하고 빈/짧은 keyword 는 빈 목록 반환. (개인정보 노출 최소화, `docs/security.md`)
- [src/main/java/AirPort/controller/KioskController.java:44](src/main/java/AirPort/controller/KioskController.java) 공개 조회(`/acGroups`, `/managers`, `/codes`)는 **감사 로그가 없다**(무인증이라 user 없음). 저장(`create`)은 `auditService.log(null, CREATE, …)` 로 남는다. §4 감사 대상(조회) 관점에서 공개 조회도 최소한의 접근 흔적(요청 수/IP 등)을 남길지 검토. 현재 감사 모델이 user 중심이라 강제는 아님. (`docs/security.md`)
- [src/main/java/AirPort/controller/KioskController.java:63](src/main/java/AirPort/controller/KioskController.java) `POST /kiosk/visit` 는 **무인증 쓰기**(tb_visit·tb_person·tb_car 생성)라 스팸/남용에 노출된다. 운영 배포 시 키오스크 PC 로 접근을 제한하거나(네트워크/방화벽), 간단한 rate-limit·CAPTCHA 를 검토. 기능상 의도된 공개지만 남용 방지 수단이 없다. (`docs/security.md`)
- 테스트 없음 — `KioskVisitService.create` 의 필수값 검증(작업기간/인솔자/방문구역/방문객/차량구역↔차량)과 `searchManagersPublic` 이름검색은 회귀 위험이 있는 로직. 최소 단위테스트 권고. (`docs/testing.md`)

## 🟢 양호
- **계층 경계**: `KioskController → KioskVisitService → Mapper` 단방향. 컨트롤러에 SQL·비즈니스 로직 없음(서비스만 호출). ([KioskController.java](src/main/java/AirPort/controller/KioskController.java))
- **MyBatis 규약**: 신규/변경 쿼리(`selectRegular`, `selectCodesForPicker` 재사용) 모두 mapper XML, 파라미터 `#{}` 만, 문자열연결·`${}`·JPA 없음. ([TbPersonMapper.xml](src/main/resources/mapper/TbPersonMapper.xml))
- **외부 연동 격리**: 키오스크는 BiostarX/adapter 를 직접 호출하지 않음(카드·연동은 관리자 카드 발급 단계로 위임 — 의도된 설계). ([KioskVisitService.java:65](src/main/java/AirPort/service/KioskVisitService.java))
- **개인정보 암호화(at rest)**: 방문객 성명·생년월일은 `upsertVisitor` 에서 ARIA 암호화 저장. ([VisitService.java:344](src/main/java/AirPort/service/VisitService.java))
- **비밀값**: 커밋 대상에 하드코딩 접속정보/키 없음. 실제 접속정보·키스토어(`application-local.properties`, `certs/`)는 `.gitignore` 로 제외.
- **관례/공개경로**: `/kiosk/**` 를 `WebConfig.EXCLUDES` 로 인증·메뉴통제에서 제외하고 `docs/security.md` 에 명시. 명명·배치(KioskController/KioskVisitService)가 관례 준수. ([WebConfig.java:16](src/main/java/AirPort/config/WebConfig.java))

## 참고 (경계 아님)
- `VisitService` 재사용 헬퍼(`toRow`/`upsertVisitor`/`insertVisitCar`/`pruneAreaScope`)·상수를 package-private 로 열어 `KioskVisitService` 가 재사용 — 결합도는 올라가나 중복을 피한 합리적 선택. 향후 방문 도메인이 더 커지면 공용 도메인 헬퍼를 별도 컴포넌트로 추출 검토.

## 조치
- 🟡-1(무인증 명단 노출): (대기) 사용자 승인 후 서버 최소 검색어 강제 반영 예정.
- 🟡-2(공개 조회 감사): (보류) 감사 모델 user 중심 — 별도 논의.
- 🟡-3(무인증 쓰기 남용): (보류) 운영 배포 시 접근 제한으로 대응.
- 🟡-4(테스트): (대기) 후속 커밋에서 추가 검토.
