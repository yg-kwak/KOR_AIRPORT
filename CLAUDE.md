# CJAirPort — Claude Code 지침

이 저장소의 **단일 진실 원천(single source of truth)** 은 `AGENTS.md` 입니다.
먼저 지도(AGENTS.md)를 읽고, 상세는 `docs/` 에서 필요한 문서만 펼쳐 읽으세요 (프로그레시브 디스클로저).

- 불변식(강제 규칙)과 저장소 지도: **AGENTS.md**
- 상세 지식: **docs/** (색인: `docs/README.md`)
- 반복 작업: **`.claude/commands/`** (`/commit`, `/review`, `/deploy`, `/cleanup`, `/new-screen`)

@AGENTS.md
