<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="AirPort.mapper.TbMenuMapper">

  <select id="selectUseList" resultType="TbMenu">
    SELECT menu_id, menu_name, parent_menu_id, menu_url, menu_level, menu_order, use_yn
    FROM tb_menu
    WHERE use_yn = 'Y'
    ORDER BY menu_level, menu_order, menu_id
  </select>

</mapper>
